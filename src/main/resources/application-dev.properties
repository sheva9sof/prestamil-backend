# ── Dev profile ──────────────────────────────────────────────────────────────
# Only non-sensitive dev settings belong here.

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
