---
phase: 260522-g33-actualizar-mock-de-avaluos-prendarios-re
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prestamil-frontend/src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.ts
  - prestamil-frontend/src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.html
  - prestamil-frontend/src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.scss
  - .planning/codebase/CONCERNS.md
autonomous: false
requirements:
  - QUICK-260522-g33
must_haves:
  truths:
    - "El header del mock muestra usuario y rol reales tomados de AuthService (no hardcodeados)"
    - "El select de Plazo se llena con datos reales de PlazoService.getAll() — con fallback estático si la llamada falla"
    - "Al seleccionar tipo ALHAJAS/PLATA, el formulario muestra los campos: Prenda (con búsqueda modal), Hechura, Kilataje, Cantidad, Peso, Color, Claridad, Precio x gramo (auto), Avalúo real (auto), Avalúo contrato (auto), Préstamo (auto)"
    - "Al seleccionar tipo VARIOS, el formulario muestra Subtipo, Marca, Modelo, Serie/IMEI, Estado, Préstamo manual, Avalúo contrato (auto = prestamo * 1.50)"
    - "Al seleccionar tipo AUTOS/MOTOS, el formulario muestra un mensaje 'no disponible' y se deshabilita el botón Agregar partida"
    - "El botón 'Buscar cliente' abre un NgbModal con lista filtrable de clientes mock; seleccionar uno llena nombre/folio/identificación/préstamoAcumulado"
    - "El botón 'Prenda' (en ALHAJAS/PLATA) abre un NgbModal con catálogo filtrable y auto-llena descripción/tipo/kilataje al seleccionar"
    - "El botón 'Agregar partida' valida cliente, plazo, tipo no-vehículo y peso/préstamo > 0; muestra errorMessage si falla, agrega fila + successMessage 'Partida agregada correctamente' (auto-dismiss 3s) si pasa"
    - "La tabla de partidas incluye columna 'Avalúo contrato' además de las existentes; el resumen rápido incluye avaluoContratoTotal"
    - "El botón 'Generar contrato' abre un NgbModal de confirmación con resumen (cliente, plazo, # partidas, préstamo total, avalúo contrato total); confirmar limpia partidas + cliente y muestra successMessage demo"
    - "El botón 'Ver vencimientos' abre un NgbModal con tabla mock de vencimientos (Contrato, Fecha inicio, Fecha límite, Monto, Estatus); título incluye nombre del cliente seleccionado"
    - "Se eliminó toda llamada a alert() y console.log de producción del componente"
    - "successMessage/errorMessage siguen el patrón de empresas.component.ts (Bootstrap alerts, auto-dismiss del success a 3s)"
    - "Una tarjeta 'Pendiente confirmar' colapsable es visible solo cuando !environment.production, con las 5 preguntas de negocio listadas"
    - "CONCERNS.md refleja el estado actual: Avaluos pasa de 'mock UI only, zero backend' a 'mock UI funcional con flujo completo de captura, cálculos locales y validaciones. Backend pendiente.'"
    - "CONCERNS.md tiene nueva sección 'Avaluos — decisiones pendientes de negocio' con las 5 preguntas"
  artifacts:
    - path: "prestamil-frontend/src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.ts"
      provides: "Componente Angular standalone con AuthService injection, PlazoService injection (con fallback), modales NgbModal para cliente/prenda/contrato/vencimientos, cálculos automáticos de oro, validaciones por tipo, successMessage/errorMessage pattern"
      contains: "inject(AuthService), inject(NgbModal), inject(PlazoService), preciosMockOro, prendasMock, clientesMock, plazosDemo, calcularPrecioPrestamo, calcularAvaluoContrato, agregarPartida, generarContrato, verVencimientos, abrirBuscarCliente, abrirBuscarPrenda"
    - path: "prestamil-frontend/src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.html"
      provides: "Template con secciones de cliente (con buscador), plazo (select), formularios reactivos por tipo (ngSwitch ALHAJAS/PLATA/VARIOS/AUTOS), tabla extendida con columna Avalúo contrato, ng-template para los 4 modales, tarjeta colapsable Pendiente confirmar"
      contains: "(click)=\"abrirBuscarCliente()\", (click)=\"abrirBuscarPrenda()\", (click)=\"agregarPartida()\", (click)=\"generarContrato()\", (click)=\"verVencimientos()\", ng-template, ngbCollapse, *ngIf=\"!environment.production\""
    - path: "prestamil-frontend/src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.scss"
      provides: "Estilos existentes preservados + estilos nuevos para inputs con botón de búsqueda inline, modales y tarjeta pendientes"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Sección Avaluos actualizada, nueva sección decisiones pendientes, Development Progress actualizado"
  key_links:
    - from: "avaluo-mock.component.ts"
      to: "AuthService.getUser()"
      via: "inject(AuthService) + getUser()?.nombre/rol"
      pattern: "this\\.authService\\.getUser\\(\\)"
    - from: "avaluo-mock.component.ts"
      to: "PlazoService.getAll()"
      via: "inject(PlazoService) en ngOnInit con subscribe/catchError"
      pattern: "this\\.plazoService\\.getAll\\(\\)"
    - from: "avaluo-mock.component.ts"
      to: "NgbModal.open()"
      via: "inject(NgbModal) para 4 modales: cliente, prenda, contrato confirmación, vencimientos"
      pattern: "this\\.modalService\\.open\\("
    - from: "avaluo-mock.component.html"
      to: "componente partidas + cálculos"
      via: "(click)=agregarPartida(), validaciones por tipoSeleccionado"
      pattern: "agregarPartida\\(\\)"
---

<objective>
Reescribir el mock visual del componente `avaluo-mock` para reflejar el flujo de negocio real de avalúos prendarios sin tocar backend.

Purpose: El mock actual usa inputs hardcodeados, `alert()`, `console.log` y placeholders de texto que no se parecen al flujo final. La intención es que el cliente (Aimee) pueda validar el flujo completo (búsqueda de cliente, captura por tipo de prenda, cálculos automáticos de oro, generación de contrato demo, vencimientos) en una sesión de demo sin necesidad de backend. También se desbloquea el camino para conectar las APIs reales cuando existan.

Output: Componente standalone funcional con AuthService real, PlazoService real (con fallback), 4 modales NgbModal, validaciones de flujo, cálculos automáticos basados en precios mock de oro, tarjeta de preguntas pendientes visible solo en dev, y CONCERNS.md actualizado para reflejar el nuevo estado.

Scope strictly frontend mock — NO backend endpoints, NO Refrendos/Finiquitos/Abonos, NO route/guard changes, NO PDF real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@prestamil-frontend/src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.ts
@prestamil-frontend/src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.html
@prestamil-frontend/src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.scss
@prestamil-frontend/src/app/prestamil/core/services/auth.service.ts
@prestamil-frontend/src/app/prestamil/core/services/plazo.service.ts
@prestamil-frontend/src/app/prestamil/pages/catalogos/empresas/empresas.component.ts
@prestamil-frontend/src/environments/environment.ts

<interfaces>
<!-- Contratos clave que el executor necesita. NO explorar codebase para esto. -->

From core/services/auth.service.ts:
```typescript
@Injectable({ providedIn: 'root' })
export class AuthService {
  // Devuelve el JSON guardado en localStorage AUTH_USER_KEY, o null
  getUser(): any;  // shape: { id, nombreUsuario, nombre, apellidos, idRol }
  getUserId(): number | null;
  isAuthenticated(): boolean;
}
```
NOTA: AuthService NO expone `sucursalId` ni `rol` como string. Para `rol` mostrar `idRol` o "Sin rol" si no existe. Para `sucursalId` hardcodear `1` con comentario TODO (alineado con la decisión 2026-05-16 en STATE.md).

From core/services/plazo.service.ts:
```typescript
@Injectable({ providedIn: 'root' })
export class PlazoService {
  getAll(): Observable<PlazoResponse[]>;
  // ... otros métodos no necesarios aquí
}
```

From core/models/plazo.model.ts (PlazoResponse shape — confirmar al leer):
```typescript
interface PlazoResponse {
  id: number;
  nombre: string;
  diasPorPeriodo?: number;
  numeroPeriodos?: number;
  activo?: boolean;
  // ... otros campos
}
```
Si PlazoResponse no expone `diasPorPeriodo`/`numeroPeriodos` con esos nombres exactos, leer el modelo y mapear; mostrar mínimamente `nombre` en el `<option>` y guardar el objeto completo en `plazoSeleccionado`.

From @ng-bootstrap/ng-bootstrap (ya re-exportado por SharedModule):
```typescript
class NgbModal {
  open(content: TemplateRef<any> | Type<any>, options?: NgbModalOptions): NgbModalRef;
}
class NgbModalRef {
  close(result?: any): void;
  dismiss(reason?: any): void;
  result: Promise<any>;
}
```
Usar `@ViewChild('templateRef') templateRef!: TemplateRef<any>` + `this.modalService.open(this.templateRef, { size: 'lg' })`.

From SharedModule (theme/shared/shared.module.ts):
Ya re-exporta CommonModule, FormsModule, ReactiveFormsModule, CardComponent, NgbModule, NgScrollbarModule. No agregar imports extras al @Component standalone más allá de CommonModule + SharedModule + FormsModule (para ngModel en inputs).

Patrón de mensajes de empresas.component.ts (referencia):
```typescript
successMessage = '';
errorMessage = '';
// Después de éxito:
this.successMessage = '...';
setTimeout(() => (this.successMessage = ''), 3000);
// Después de error:
this.errorMessage = '...';
```
Template:
```html
<div *ngIf="successMessage" class="alert alert-success">{{ successMessage }}</div>
<div *ngIf="errorMessage" class="alert alert-danger">{{ errorMessage }}</div>
```

From environments/environment.ts:
```typescript
export const environment = { production: ..., apiUrl: ..., appVersion: ... };
```
Importar como `import { environment } from 'src/environments/environment';` y exponer como propiedad pública `environment = environment;` para usar en template con `*ngIf="!environment.production"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reescribir avaluo-mock.component.ts (TypeScript)</name>
  <files>prestamil-frontend/src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.ts</files>
  <behavior>
    Comportamientos verificables del componente reescrito:
    - Al construirse, `usuario`/`rol` provienen de `authService.getUser()` (no hardcodeados); si no hay sesión, fallback 'Sin sesión'/''
    - `sucursalId` hardcodeado a 1 con `// TODO: leer de TurnoService/sesión activa` (alineado a STATE.md decisión 2026-05-16)
    - `ngOnInit` llama `plazoService.getAll().subscribe(...)` y, en caso de error, asigna `plazos = plazosDemo` (fallback estático)
    - `seleccionarTipo('Autos/Motos')` deja `puedeAgregarPartida = false`
    - `seleccionarTipo('Alhajas')` o `seleccionarTipo('Plata')` deja `puedeAgregarPartida = true`
    - Cambiar `kilataje` o `peso` recalcula automáticamente `precioXGramo`, `avaluoReal`, `prestamo`, `avaluoContrato` usando la fórmula mock
    - Para tipo Varios, cambiar `prestamo` (manual) recalcula `avaluoContrato = prestamo * 1.50`
    - `agregarPartida()` sin cliente → `errorMessage = 'Selecciona un cliente antes de agregar una partida'`, no agrega fila
    - `agregarPartida()` sin plazo → errorMessage similar, no agrega fila
    - `agregarPartida()` con AUTOS/MOTOS → errorMessage, no agrega fila
    - `agregarPartida()` con ALHAJAS y peso=0 → errorMessage 'Captura un peso mayor a 0'
    - `agregarPartida()` con VARIOS y prestamo=0 → errorMessage 'Captura un préstamo mayor a 0'
    - `agregarPartida()` válido → empuja a `partidas`, limpia formulario, `successMessage = 'Partida agregada correctamente'` con auto-dismiss 3s
    - Confirmar en modal de generar contrato → limpia `partidas = []`, `clienteSeleccionado = null`, successMessage demo
    - No quedan llamadas a `alert()` ni `console.log` en el componente final
  </behavior>
  <action>
    Reescribir COMPLETAMENTE `avaluo-mock.component.ts`. NO Edit puntuales — Write completo.

    Estructura del componente:

    1. **Imports**:
       ```typescript
       import { CommonModule } from '@angular/common';
       import { Component, OnInit, TemplateRef, ViewChild, inject } from '@angular/core';
       import { FormsModule } from '@angular/forms';
       import { NgbModal } from '@ng-bootstrap/ng-bootstrap';
       import { SharedModule } from 'src/app/theme/shared/shared.module';
       import { AuthService } from 'src/app/prestamil/core/services/auth.service';
       import { PlazoService } from 'src/app/prestamil/core/services/plazo.service';
       import { environment } from 'src/environments/environment';
       ```

    2. **Interfaces locales** (al inicio del archivo, fuera de la clase):
       ```typescript
       interface PartidaMock {
         id: number;
         tipo: string;
         clavePrenda: string;
         descripcion: string;
         cantidad: number;
         peso: number;
         kilataje?: number;
         hechura?: string;
         precioXGramo?: number;
         avaluoReal: number;
         avaluoContrato: number;
         prestamo: number;
         vencimiento: string;
         estatus: string;
       }
       interface ClienteMock { folio: string; nombre: string; identificacion: string; prestamoAcumulado: number; }
       interface PrendaCatalogoMock { clave: string; descripcion: string; tipo: string; kilataje: number; }
       interface PlazoMock { id: number; nombre: string; diasPorPeriodo: number; numeroPeriodos: number; }
       interface VencimientoMock { contrato: string; fechaInicio: string; fechaLimite: string; monto: number; estatus: string; }
       ```

    3. **@Component decorator**:
       ```typescript
       @Component({
         selector: 'app-avaluo-mock',
         imports: [CommonModule, FormsModule, SharedModule],
         templateUrl: './avaluo-mock.component.html',
         styleUrls: ['./avaluo-mock.component.scss']
       })
       export class AvaluoMockComponent implements OnInit { ... }
       ```

    4. **Inyecciones**:
       ```typescript
       private authService = inject(AuthService);
       private plazoService = inject(PlazoService);
       private modalService = inject(NgbModal);
       ```

    5. **Propiedades de sesión** (NO `readonly` — derivadas en runtime):
       ```typescript
       readonly sucursal = 'Sucursal Centro'; // mantener por ahora
       usuario = this.authService.getUser()?.nombre ?? 'Sin sesión';
       rol = this.authService.getUser()?.idRol ? `Rol ${this.authService.getUser().idRol}` : '';
       sucursalId = 1; // TODO: leer de TurnoService/sesión activa cuando exista
       readonly fechaAbierta = new Date().toLocaleDateString('es-MX');
       operacionAbierta = true;
       readonly environment = environment;
       ```

    6. **Mensajes** (patrón empresas.component.ts):
       ```typescript
       successMessage = '';
       errorMessage = '';
       private mostrarExito(msg: string): void {
         this.successMessage = msg;
         this.errorMessage = '';
         setTimeout(() => (this.successMessage = ''), 3000);
       }
       private mostrarError(msg: string): void {
         this.errorMessage = msg;
         this.successMessage = '';
       }
       ```

    7. **Tipos de prenda + flag**:
       ```typescript
       tiposPrenda = ['Alhajas', 'Plata', 'Varios', 'Autos/Motos'];
       tipoSeleccionado = 'Alhajas';
       puedeAgregarPartida = true;
       ```

    8. **Plazos**: estado + fallback estático
       ```typescript
       plazos: PlazoMock[] = [];
       plazoSeleccionado: PlazoMock | null = null;
       private readonly plazosDemo: PlazoMock[] = [
         { id: 1, nombre: 'ALHAJAS - 12 SEMANAS', diasPorPeriodo: 7, numeroPeriodos: 12 },
         { id: 2, nombre: 'ALHAJAS - 10 SEMANAS', diasPorPeriodo: 7, numeroPeriodos: 10 },
         { id: 3, nombre: 'QUINCENAL',            diasPorPeriodo: 15, numeroPeriodos: 6 },
         { id: 4, nombre: 'MENSUAL',              diasPorPeriodo: 30, numeroPeriodos: 6 }
       ];
       ```
       En `ngOnInit`:
       ```typescript
       this.plazoService.getAll().subscribe({
         next: (lista) => {
           // mapear lo que devuelve el backend al shape mock
           this.plazos = (lista || []).map(p => ({
             id: p.id,
             nombre: p.nombre,
             diasPorPeriodo: (p as any).diasPorPeriodo ?? 7,
             numeroPeriodos: (p as any).numeroPeriodos ?? 1
           }));
           if (this.plazos.length === 0) this.plazos = this.plazosDemo;
         },
         error: () => { this.plazos = this.plazosDemo; }
       });
       ```
       Función helper para el label del `<option>`:
       ```typescript
       labelPlazo(p: PlazoMock): string {
         return `${p.nombre} - ${p.diasPorPeriodo * p.numeroPeriodos} días`;
       }
       ```

    9. **Catálogos mock** (al final, sección de datos mock):
       ```typescript
       readonly prendasMock: PrendaCatalogoMock[] = [
         { clave: 'AHD10', descripcion: 'AHOGADOR ORO 10K', tipo: 'ALHAJAS', kilataje: 10 },
         { clave: 'AHD12', descripcion: 'AHOGADOR ORO 12K', tipo: 'ALHAJAS', kilataje: 12 },
         { clave: 'AHD14', descripcion: 'AHOGADOR ORO 14K', tipo: 'ALHAJAS', kilataje: 14 },
         { clave: 'AHD18', descripcion: 'AHOGADOR ORO 18K', tipo: 'ALHAJAS', kilataje: 18 },
         { clave: 'AHD21', descripcion: 'AHOGADOR ORO 21K', tipo: 'ALHAJAS', kilataje: 21 },
         { clave: 'ANL14', descripcion: 'ANILLO ORO 14K',   tipo: 'ALHAJAS', kilataje: 14 },
         { clave: 'PUL18', descripcion: 'PULSERA ORO 18K',  tipo: 'ALHAJAS', kilataje: 18 },
         { clave: 'ARE14', descripcion: 'ARETES ORO 14K',   tipo: 'ALHAJAS', kilataje: 14 }
       ];
       readonly clientesMock: ClienteMock[] = [
         { folio: 'CLI-000001', nombre: 'CRISTAL ZACARIAS MEZA',  identificacion: 'CREDENCIAL IFE', prestamoAcumulado: 0 },
         { folio: 'CLI-000027', nombre: 'ARSEMIA MANZO BAUTISTA', identificacion: 'CREDENCIAL IFE', prestamoAcumulado: 3200 },
         { folio: 'CLI-000038', nombre: 'ALBERTO PEREZ CAMPOS',   identificacion: 'CREDENCIAL IFE', prestamoAcumulado: 7748 },
         { folio: 'CLI-000245', nombre: 'MARIA FERNANDA LOPEZ',   identificacion: 'CREDENCIAL IFE', prestamoAcumulado: 8500 }
       ];
       readonly tiposIdentificacion = ['CREDENCIAL IFE', 'PASAPORTE', 'CÉDULA PROFESIONAL', 'LICENCIA DE MANEJO', 'CARTILLA S.M.N.'];
       readonly kilatajes = [6, 8, 10, 12, 14, 18, 21, 24];
       readonly hechuras = ['FUNDIR', 'NORMAL', 'ESPECIAL'];
       readonly subtiposVarios = ['Electrodoméstico', 'Celular', 'Laptop', 'Otro'];
       readonly estadosVarios = ['Bueno', 'Regular', 'Malo'];
       readonly preciosMockOro: Record<number, number> = {
         6: 118.84, 8: 384.65, 10: 488.26, 12: 591.38, 14: 695.74, 18: 900.01, 21: 1052.50, 24: 0
       };
       private readonly porcAumento = 3.0;
       private readonly porcIncrementoAvaluo = 50.0;
       readonly vencimientosMock: VencimientoMock[] = [
         { contrato: 'CTR-100231', fechaInicio: '01/04/2026', fechaLimite: '01/05/2026', monto: 4200, estatus: 'Vigente' },
         { contrato: 'CTR-100244', fechaInicio: '15/03/2026', fechaLimite: '14/04/2026', monto: 2800, estatus: 'Vencido' },
         { contrato: 'CTR-100279', fechaInicio: '02/05/2026', fechaLimite: '01/06/2026', monto: 6500, estatus: 'Vigente' }
       ];
       ```

    10. **Estado cliente seleccionado**:
        ```typescript
        clienteSeleccionado: ClienteMock | null = null;
        beneficiario = '';
        identificacionSeleccionada = 'CREDENCIAL IFE';
        filtroCliente = '';
        ```

    11. **Estado de captura ALHAJAS/PLATA**:
        ```typescript
        captura = {
          clavePrenda: '',
          descripcion: '',
          hechura: 'NORMAL',
          kilataje: 14,
          cantidad: 1,
          peso: 0,
          color: '',
          claridad: '',
          precioXGramo: 0,
          avaluoReal: 0,
          avaluoContrato: 0,
          prestamo: 0
        };
        filtroPrenda = '';
        ```

    12. **Estado de captura VARIOS**:
        ```typescript
        capturaVarios = {
          subtipo: 'Celular',
          marca: '',
          modelo: '',
          serie: '',
          estado: 'Bueno',
          prestamo: 0,
          avaluoContrato: 0
        };
        ```

    13. **Cálculos automáticos** (llamados desde el template con (ngModelChange) o getters):
        ```typescript
        recalcularAlhajas(): void {
          const precioBase = this.preciosMockOro[this.captura.kilataje] ?? 0;
          this.captura.precioXGramo = precioBase;
          this.captura.prestamo = +(precioBase * this.captura.peso * (1 + this.porcAumento / 100)).toFixed(2);
          this.captura.avaluoReal = +(this.captura.prestamo).toFixed(2);
          this.captura.avaluoContrato = +(this.captura.prestamo * (1 + this.porcIncrementoAvaluo / 100)).toFixed(2);
        }
        recalcularVarios(): void {
          this.capturaVarios.avaluoContrato = +(this.capturaVarios.prestamo * 1.50).toFixed(2);
        }
        ```

    14. **Partidas + getters**:
        ```typescript
        partidas: PartidaMock[] = []; // empezar vacío (sin demo data)
        get totalPartidas(): number { return this.partidas.length; }
        get pesoTotal(): number { return this.partidas.reduce((a, i) => a + i.peso, 0); }
        get avaluoTotal(): number { return this.partidas.reduce((a, i) => a + i.avaluoReal, 0); }
        get avaluoContratoTotal(): number { return this.partidas.reduce((a, i) => a + i.avaluoContrato, 0); }
        get prestamoTotal(): number { return this.partidas.reduce((a, i) => a + i.prestamo, 0); }
        ```

    15. **Acciones**:
        ```typescript
        seleccionarTipo(tipo: string): void {
          this.tipoSeleccionado = tipo;
          this.puedeAgregarPartida = tipo !== 'Autos/Motos';
        }

        agregarPartida(): void {
          if (!this.clienteSeleccionado) { this.mostrarError('Selecciona un cliente antes de agregar una partida'); return; }
          if (!this.plazoSeleccionado) { this.mostrarError('Selecciona un plazo antes de agregar una partida'); return; }
          if (this.tipoSeleccionado === 'Autos/Motos') { this.mostrarError('Autos/Motos no está disponible en esta versión'); return; }

          if (this.tipoSeleccionado === 'Alhajas' || this.tipoSeleccionado === 'Plata') {
            if (this.captura.peso <= 0) { this.mostrarError('Captura un peso mayor a 0'); return; }
            const nueva: PartidaMock = {
              id: this.partidas.length + 1,
              tipo: this.tipoSeleccionado,
              clavePrenda: this.captura.clavePrenda || '—',
              descripcion: this.captura.descripcion || `${this.tipoSeleccionado} ${this.captura.kilataje}K`,
              cantidad: this.captura.cantidad,
              peso: this.captura.peso,
              kilataje: this.captura.kilataje,
              hechura: this.captura.hechura,
              precioXGramo: this.captura.precioXGramo,
              avaluoReal: this.captura.avaluoReal,
              avaluoContrato: this.captura.avaluoContrato,
              prestamo: this.captura.prestamo,
              vencimiento: this.calcularVencimiento(),
              estatus: 'Capturada'
            };
            this.partidas = [...this.partidas, nueva];
            this.resetCapturaAlhajas();
          } else if (this.tipoSeleccionado === 'Varios') {
            if (this.capturaVarios.prestamo <= 0) { this.mostrarError('Captura un préstamo mayor a 0'); return; }
            const nueva: PartidaMock = {
              id: this.partidas.length + 1,
              tipo: 'Varios',
              clavePrenda: this.capturaVarios.subtipo.substring(0, 3).toUpperCase(),
              descripcion: `${this.capturaVarios.subtipo} ${this.capturaVarios.marca} ${this.capturaVarios.modelo}`.trim(),
              cantidad: 1,
              peso: 0,
              avaluoReal: this.capturaVarios.prestamo,
              avaluoContrato: this.capturaVarios.avaluoContrato,
              prestamo: this.capturaVarios.prestamo,
              vencimiento: this.calcularVencimiento(),
              estatus: 'Capturada'
            };
            this.partidas = [...this.partidas, nueva];
            this.resetCapturaVarios();
          }
          this.mostrarExito('Partida agregada correctamente');
        }

        private calcularVencimiento(): string {
          if (!this.plazoSeleccionado) return '—';
          const dias = this.plazoSeleccionado.diasPorPeriodo * this.plazoSeleccionado.numeroPeriodos;
          const fecha = new Date();
          fecha.setDate(fecha.getDate() + dias);
          return fecha.toLocaleDateString('es-MX');
        }

        private resetCapturaAlhajas(): void {
          this.captura = { ...this.captura, clavePrenda: '', descripcion: '', cantidad: 1, peso: 0, color: '', claridad: '', precioXGramo: 0, avaluoReal: 0, avaluoContrato: 0, prestamo: 0 };
        }
        private resetCapturaVarios(): void {
          this.capturaVarios = { subtipo: 'Celular', marca: '', modelo: '', serie: '', estado: 'Bueno', prestamo: 0, avaluoContrato: 0 };
        }

        eliminarPartida(id: number): void {
          this.partidas = this.partidas.filter(p => p.id !== id);
        }
        ```

    16. **Modales — usar @ViewChild + TemplateRef**:
        ```typescript
        @ViewChild('modalCliente') modalCliente!: TemplateRef<any>;
        @ViewChild('modalPrenda') modalPrenda!: TemplateRef<any>;
        @ViewChild('modalContrato') modalContrato!: TemplateRef<any>;
        @ViewChild('modalVencimientos') modalVencimientos!: TemplateRef<any>;

        abrirBuscarCliente(): void {
          this.filtroCliente = '';
          this.modalService.open(this.modalCliente, { size: 'lg' });
        }
        seleccionarCliente(c: ClienteMock, modal: any): void {
          this.clienteSeleccionado = c;
          this.identificacionSeleccionada = c.identificacion;
          modal.close();
        }
        get clientesFiltrados(): ClienteMock[] {
          const q = this.filtroCliente.trim().toLowerCase();
          if (!q) return this.clientesMock;
          return this.clientesMock.filter(c =>
            c.folio.toLowerCase().includes(q) || c.nombre.toLowerCase().includes(q));
        }

        abrirBuscarPrenda(): void {
          if (this.tipoSeleccionado !== 'Alhajas' && this.tipoSeleccionado !== 'Plata') return;
          this.filtroPrenda = '';
          this.modalService.open(this.modalPrenda, { size: 'lg' });
        }
        seleccionarPrenda(p: PrendaCatalogoMock, modal: any): void {
          this.captura.clavePrenda = p.clave;
          this.captura.descripcion = p.descripcion;
          this.captura.kilataje = p.kilataje;
          this.recalcularAlhajas();
          modal.close();
        }
        get prendasFiltradas(): PrendaCatalogoMock[] {
          const q = this.filtroPrenda.trim().toLowerCase();
          if (!q) return this.prendasMock;
          return this.prendasMock.filter(p =>
            p.clave.toLowerCase().includes(q) || p.descripcion.toLowerCase().includes(q));
        }

        generarContrato(): void {
          if (this.partidas.length === 0) { this.mostrarError('Agrega al menos una partida antes de generar el contrato'); return; }
          if (!this.clienteSeleccionado) { this.mostrarError('Selecciona un cliente antes de generar el contrato'); return; }
          this.modalService.open(this.modalContrato, { size: 'md' }).result.then(
            () => {
              // Confirmado
              this.partidas = [];
              this.clienteSeleccionado = null;
              this.beneficiario = '';
              this.mostrarExito('Contrato generado correctamente (demo). En producción se generará el PDF.');
            },
            () => { /* dismissed */ }
          );
        }

        verVencimientos(): void {
          if (!this.clienteSeleccionado) { this.mostrarError('Selecciona un cliente para ver sus vencimientos'); return; }
          this.modalService.open(this.modalVencimientos, { size: 'lg' });
        }
        ```

    17. **Tarjeta pendientes**:
        ```typescript
        pendientesAbiertas = false;
        togglePendientes(): void { this.pendientesAbiertas = !this.pendientesAbiertas; }
        ```

    PROHIBIDO en el archivo final:
    - alert()
    - console.log / console.debug
    - any campo readonly que dependa de runtime (usuario, rol, fechaAbierta calculada)
    - `simularAccion`, `simularAgregarPartida`, `simularGenerarContrato`, `mensajeFlujo` (eliminar)

    Cumple convención del proyecto: standalone component (matches `quinjos-periodos` y `parametros-prestamo` modernos), inject() pattern, NO `I` prefix en interfaces, camelCase, Spanish domain names.
  </action>
  <verify>
    <automated>cd prestamil-frontend && npx ng build --configuration development 2>&1 | tail -40</automated>
  </verify>
  <done>
    - `avaluo-mock.component.ts` compila sin errores
    - No quedan referencias a `alert(`, `console.log(`, `simularAccion`, `simularAgregarPartida`, `simularGenerarContrato`, ni `mensajeFlujo`
    - `inject(AuthService)`, `inject(PlazoService)`, `inject(NgbModal)` presentes
    - Interfaces locales `PartidaMock`, `ClienteMock`, `PrendaCatalogoMock`, `PlazoMock`, `VencimientoMock` declaradas
    - Métodos `agregarPartida`, `generarContrato`, `verVencimientos`, `abrirBuscarCliente`, `abrirBuscarPrenda`, `recalcularAlhajas`, `recalcularVarios` definidos
    - `@ViewChild` para los 4 modales presentes
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reescribir avaluo-mock.component.html (template) + ajustes SCSS</name>
  <files>prestamil-frontend/src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.html, prestamil-frontend/src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.scss</files>
  <behavior>
    - El template usa los nuevos campos del componente (no quedan referencias a `mensajeFlujo`, `clienteDemo`, `simularAccion`, etc.)
    - Las 4 secciones de modal existen como `<ng-template #modalCliente let-modal>`, etc.
    - El tipo seleccionado controla qué grupo de campos se renderiza vía `*ngIf="tipoSeleccionado === '...'"`
    - El select de Plazo itera `*ngFor="let p of plazos"` y muestra `labelPlazo(p)`
    - El input de cliente tiene botón con ícono de búsqueda (texto "Buscar" o feather-icon `<i class="feather icon-search"></i>` si el proyecto los usa — usar texto simple si hay duda)
    - El botón "Agregar partida" está deshabilitado cuando `!puedeAgregarPartida`
    - La tabla incluye la columna "Avalúo contrato" entre "Avaluo real" y "Prestamo"
    - El resumen rápido incluye "Avaluo contrato total"
    - La tarjeta "Pendiente confirmar" usa `*ngIf="!environment.production"` y muestra las 5 preguntas
  </behavior>
  <action>
    Reescribir COMPLETAMENTE `avaluo-mock.component.html` (Write, no Edit). Estructura:

    1. **Encabezado de página** (mantener estilo actual con badge operacionAbierta).

    2. **Card 0 — header de sesión**: Sucursal / Usuario / Rol / Fecha. Reemplazar `{{ usuario }}` (string hardcodeado) por la nueva propiedad (que viene de AuthService).

    3. **Alerts de mensajes** (reemplazar alert único de `mensajeFlujo`):
       ```html
       <div *ngIf="successMessage" class="alert alert-success">{{ successMessage }}</div>
       <div *ngIf="errorMessage" class="alert alert-danger">{{ errorMessage }}</div>
       ```

    4. **Card 1 — Datos de cliente/pignorante**:
       ```html
       <app-card cardTitle="1) Datos de cliente/pignorante">
         <div class="form-row">
           <div class="form-group col-md-6">
             <label>Buscar cliente</label>
             <div class="input-group">
               <input class="form-control" readonly
                      [value]="clienteSeleccionado ? clienteSeleccionado.folio + ' - ' + clienteSeleccionado.nombre : 'Sin cliente seleccionado'" />
               <div class="input-group-append">
                 <button type="button" class="btn btn-outline-primary" (click)="abrirBuscarCliente()">Buscar</button>
               </div>
             </div>
           </div>
           <div class="form-group col-md-3">
             <label>Tipo identificación</label>
             <select class="form-control" [(ngModel)]="identificacionSeleccionada">
               <option *ngFor="let t of tiposIdentificacion" [value]="t">{{ t }}</option>
             </select>
           </div>
           <div class="form-group col-md-3">
             <label>Folio</label>
             <input class="form-control" readonly [value]="clienteSeleccionado?.folio || ''" />
           </div>
         </div>
         <div class="form-row">
           <div class="form-group col-md-6">
             <label>Nombre</label>
             <input class="form-control" readonly [value]="clienteSeleccionado?.nombre || ''" />
           </div>
           <div class="form-group col-md-6">
             <label>Beneficiario (opcional)</label>
             <input class="form-control" [(ngModel)]="beneficiario" />
           </div>
         </div>
         <div>
           <strong>Préstamo acumulado:</strong>
           <span>${{ (clienteSeleccionado?.prestamoAcumulado || 0) | number: '1.0-0' }}</span>
         </div>
       </app-card>
       ```

    5. **Card 2 — Resumen rápido** (agregar avaluoContratoTotal):
       ```html
       <ul class="summary-list">
         <li><span>Partidas:</span><strong>{{ totalPartidas }}</strong></li>
         <li><span>Peso total:</span><strong>{{ pesoTotal | number: '1.1-1' }} gr</strong></li>
         <li><span>Avalúo real total:</span><strong>${{ avaluoTotal | number: '1.0-0' }}</strong></li>
         <li><span>Avalúo contrato total:</span><strong>${{ avaluoContratoTotal | number: '1.0-0' }}</strong></li>
         <li><span>Préstamo total:</span><strong>${{ prestamoTotal | number: '1.0-0' }}</strong></li>
       </ul>
       ```

    6. **Card 3 — Captura de prenda**:
       Botones de tipo (igual al actual pero con `(click)="seleccionarTipo(tipo)"`).
       Selector de Plazo:
       ```html
       <div class="form-row">
         <div class="form-group col-md-12">
           <label>Plazo</label>
           <select class="form-control" [(ngModel)]="plazoSeleccionado">
             <option [ngValue]="null">— Selecciona un plazo —</option>
             <option *ngFor="let p of plazos" [ngValue]="p">{{ labelPlazo(p) }}</option>
           </select>
         </div>
       </div>
       ```

       **Bloque ALHAJAS/PLATA** (`*ngIf="tipoSeleccionado === 'Alhajas' || tipoSeleccionado === 'Plata'"`):
       Inputs para Prenda (con botón Buscar abriendo modalPrenda), Hechura (select), Kilataje (select), Cantidad (number), Peso (number, con (ngModelChange)="recalcularAlhajas()"), Color (text), Claridad (text), Precio x gramo (number readonly), Avalúo real (number readonly), Avalúo contrato (number readonly), Préstamo (number readonly).

       **Bloque VARIOS** (`*ngIf="tipoSeleccionado === 'Varios'"`):
       Subtipo (select), Marca (text), Modelo (text), Serie/IMEI (text), Estado (select), Préstamo (number con (ngModelChange)="recalcularVarios()"), Avalúo contrato (number readonly). Texto informativo: "El avalúo se calcula automáticamente sobre el monto del préstamo capturado."

       **Bloque AUTOS/MOTOS** (`*ngIf="tipoSeleccionado === 'Autos/Motos'"`):
       Mensaje en `<div class="alert alert-warning">`: "Configuración de avalúos para Autos/Motos no disponible en esta versión."

    7. **Card 4 — Acciones**:
       ```html
       <button class="btn btn-primary btn-sm" (click)="agregarPartida()" [disabled]="!puedeAgregarPartida">Agregar partida</button>
       <button class="btn btn-outline-info btn-sm" (click)="verVencimientos()">Ver vencimientos</button>
       <button class="btn btn-success btn-sm" (click)="generarContrato()">Generar contrato</button>
       ```
       (Eliminar Recalcular avaluo / Cancelar partida / Borrar partida — quedan implícitas en el flujo de captura).

    8. **Card 5 — Tabla de partidas** (agregar columna Avalúo contrato + columna acciones):
       ```html
       <table class="table table-sm table-striped table-bordered mb-0">
         <thead>
           <tr>
             <th>#</th><th>Tipo</th><th>Clave</th><th>Descripción</th><th>Cant</th>
             <th>Peso</th><th>Avalúo real</th><th>Avalúo contrato</th><th>Préstamo</th>
             <th>Vencimiento</th><th>Estatus</th><th></th>
           </tr>
         </thead>
         <tbody>
           <tr *ngFor="let item of partidas">
             <td>{{ item.id }}</td>
             <td>{{ item.tipo }}</td>
             <td>{{ item.clavePrenda }}</td>
             <td>{{ item.descripcion }}</td>
             <td>{{ item.cantidad }}</td>
             <td>{{ item.peso | number: '1.1-1' }}</td>
             <td>${{ item.avaluoReal | number: '1.0-0' }}</td>
             <td>${{ item.avaluoContrato | number: '1.0-0' }}</td>
             <td>${{ item.prestamo | number: '1.0-0' }}</td>
             <td>{{ item.vencimiento }}</td>
             <td>{{ item.estatus }}</td>
             <td><button class="btn btn-sm btn-outline-danger" (click)="eliminarPartida(item.id)">×</button></td>
           </tr>
           <tr *ngIf="partidas.length === 0">
             <td colspan="12" class="text-center text-muted">Sin partidas capturadas</td>
           </tr>
         </tbody>
       </table>
       ```

    9. **Card 6 — Pendientes (solo dev)**:
       ```html
       <div class="row" *ngIf="!environment.production">
         <div class="col-12">
           <app-card cardTitle="Pendiente confirmar (visible solo en desarrollo)">
             <button class="btn btn-sm btn-outline-secondary mb-2" (click)="togglePendientes()">
               {{ pendientesAbiertas ? 'Ocultar' : 'Mostrar' }} preguntas
             </button>
             <ul *ngIf="pendientesAbiertas">
               <li>¿El campo "Libre avalúo" permite al valuador sobreescribir el precio calculado?</li>
               <li>¿Un contrato puede tener prendas de distintos tipos (alhaja + celular)?</li>
               <li>¿El campo Beneficiario es obligatorio o siempre opcional?</li>
               <li>¿Refrendos y Finiquitos entran por esta misma pantalla o por una separada?</li>
               <li>¿Existe catálogo de precios de referencia para electrónicos o el valuador decide?</li>
             </ul>
           </app-card>
         </div>
       </div>
       ```

    10. **Modales — ng-template al final del archivo**:
        ```html
        <ng-template #modalCliente let-modal>
          <div class="modal-header">
            <h5 class="modal-title">Buscar cliente</h5>
            <button type="button" class="close" (click)="modal.dismiss()">×</button>
          </div>
          <div class="modal-body">
            <input class="form-control mb-2" [(ngModel)]="filtroCliente" placeholder="Buscar por folio o nombre" />
            <table class="table table-sm table-striped">
              <thead><tr><th>Folio</th><th>Nombre</th><th>Identificación</th><th>Préstamo acumulado</th><th></th></tr></thead>
              <tbody>
                <tr *ngFor="let c of clientesFiltrados">
                  <td>{{ c.folio }}</td><td>{{ c.nombre }}</td><td>{{ c.identificacion }}</td>
                  <td>${{ c.prestamoAcumulado | number: '1.0-0' }}</td>
                  <td><button class="btn btn-sm btn-primary" (click)="seleccionarCliente(c, modal)">Seleccionar</button></td>
                </tr>
              </tbody>
            </table>
          </div>
        </ng-template>

        <ng-template #modalPrenda let-modal>
          <div class="modal-header">
            <h5 class="modal-title">Catálogo de prendas</h5>
            <button type="button" class="close" (click)="modal.dismiss()">×</button>
          </div>
          <div class="modal-body">
            <input class="form-control mb-2" [(ngModel)]="filtroPrenda" placeholder="Buscar por clave o descripción" />
            <table class="table table-sm table-striped">
              <thead><tr><th>Clave</th><th>Descripción</th><th>Tipo</th><th>Kilataje</th><th></th></tr></thead>
              <tbody>
                <tr *ngFor="let p of prendasFiltradas">
                  <td>{{ p.clave }}</td><td>{{ p.descripcion }}</td><td>{{ p.tipo }}</td><td>{{ p.kilataje }}K</td>
                  <td><button class="btn btn-sm btn-primary" (click)="seleccionarPrenda(p, modal)">Seleccionar</button></td>
                </tr>
              </tbody>
            </table>
          </div>
        </ng-template>

        <ng-template #modalContrato let-modal>
          <div class="modal-header">
            <h5 class="modal-title">Confirmar generación de contrato</h5>
            <button type="button" class="close" (click)="modal.dismiss()">×</button>
          </div>
          <div class="modal-body">
            <p><strong>Cliente:</strong> {{ clienteSeleccionado?.nombre }}</p>
            <p><strong>Plazo:</strong> {{ plazoSeleccionado ? labelPlazo(plazoSeleccionado) : '—' }}</p>
            <p><strong>Partidas:</strong> {{ totalPartidas }}</p>
            <p><strong>Préstamo total:</strong> ${{ prestamoTotal | number: '1.0-0' }}</p>
            <p><strong>Avalúo contrato total:</strong> ${{ avaluoContratoTotal | number: '1.0-0' }}</p>
          </div>
          <div class="modal-footer">
            <button class="btn btn-secondary" (click)="modal.dismiss()">Cancelar</button>
            <button class="btn btn-success" (click)="modal.close('confirmed')">Confirmar y generar</button>
          </div>
        </ng-template>

        <ng-template #modalVencimientos let-modal>
          <div class="modal-header">
            <h5 class="modal-title">Vencimientos — {{ clienteSeleccionado?.nombre }}</h5>
            <button type="button" class="close" (click)="modal.dismiss()">×</button>
          </div>
          <div class="modal-body">
            <table class="table table-sm table-striped">
              <thead><tr><th>Contrato</th><th>Fecha inicio</th><th>Fecha límite</th><th>Monto</th><th>Estatus</th></tr></thead>
              <tbody>
                <tr *ngFor="let v of vencimientosMock">
                  <td>{{ v.contrato }}</td><td>{{ v.fechaInicio }}</td><td>{{ v.fechaLimite }}</td>
                  <td>${{ v.monto | number: '1.0-0' }}</td><td>{{ v.estatus }}</td>
                </tr>
              </tbody>
            </table>
          </div>
        </ng-template>
        ```

    **SCSS** — agregar al final del archivo existente (no tocar lo previo):
    ```scss
    .input-group .input-group-append .btn { border-top-left-radius: 0; border-bottom-left-radius: 0; }
    .pendientes-card ul { margin-bottom: 0; }
    .modal-body table { margin-bottom: 0; }
    ```

    NO eliminar las reglas existentes (`.mock-header-grid`, `.summary-list`, etc.). Mantener `.mock-fields-box` aunque ya no se use (no afecta).
  </action>
  <verify>
    <automated>cd prestamil-frontend && npx ng build --configuration development 2>&1 | tail -40</automated>
  </verify>
  <done>
    - HTML compila sin errores de template
    - No quedan referencias en el template a `mensajeFlujo`, `clienteDemo`, `simularAccion`, `simularGenerarContrato`, `simularAgregarPartida`
    - 4 `<ng-template>` presentes: #modalCliente, #modalPrenda, #modalContrato, #modalVencimientos
    - Tabla de partidas incluye `<th>Avalúo contrato</th>`
    - Resumen rápido incluye "Avalúo contrato total"
    - Tarjeta "Pendiente confirmar" envuelta en `*ngIf="!environment.production"`
    - Build de Angular completa sin warnings nuevos relevantes
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Actualizar CONCERNS.md — estado Avaluos + decisiones pendientes</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <behavior>
    - La sección "Incomplete Features / TODOs" refleja que Avaluos pasó de "mock con alerts y console.log" a "mock funcional con flujo completo de captura, cálculos locales y validaciones"
    - Existe una nueva subsección "Avaluos — decisiones pendientes de negocio" con las 5 preguntas
    - La sección "Development Progress" mueve Avaluos de "in-progress/incomplete" a un bullet aclarando "mock funcional — pendiente backend y confirmación de negocio"
  </behavior>
  <action>
    Editar `.planning/codebase/CONCERNS.md` con TRES cambios precisos:

    **Cambio 1** — En "Incomplete Features / TODOs", reemplazar el bullet actual de Avaluos:
    Buscar el bloque que empieza con "**Avaluos feature is a full mock — no real implementation:**" y reemplazar por:

    ```markdown
    **Avaluos feature is a functional mock — UI complete, backend pending:**
    - Mock UI funcional con flujo completo de captura, cálculos locales (precios de oro por kilataje) y validaciones por tipo de prenda. Conectado a AuthService real para datos de sesión y a PlazoService real para listado de plazos. Eliminados `alert()` y `console.log` de producción; reemplazados por NgbModal y patrón successMessage/errorMessage.
    - Files: `prestamil-frontend/src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.ts`, `.html`, `.scss`
    - Route registered at `avaluos/mock` — la palabra "mock" sigue en la URL hasta que exista el backend.
    - There is no backend endpoint for creating, reading, or managing loan contracts (contratos/prestamos). The business entity `Contrato` does not appear in any backend source file.
    - Pending: backend `Contrato`/`Prestamo`/`Pago` domain + endpoints; PDF real de contrato (la generación es demo); validación con cliente de las decisiones pendientes (ver siguiente sección).
    ```

    **Cambio 2** — Inmediatamente después de ese bullet (y antes del siguiente, "Hardware component is an empty shell"), insertar una nueva subsección:

    ```markdown
    **Avaluos — decisiones pendientes de negocio (documentadas en mock dev card):**
    - ¿El campo "Libre avalúo" permite al valuador sobreescribir el precio calculado por kilataje y peso?
    - ¿Un contrato puede tener prendas de distintos tipos (p. ej. una alhaja y un celular en la misma operación)?
    - ¿El campo Beneficiario es obligatorio en algún flujo o siempre opcional?
    - ¿Refrendos y Finiquitos entran por la misma pantalla de avalúos o por una pantalla separada?
    - ¿Existe catálogo de precios de referencia para electrónicos/varios, o el valuador decide manualmente cada préstamo?

    Las 5 preguntas también están visibles en una tarjeta colapsable dentro del mock (solo en `!environment.production`).
    ```

    **Cambio 3** — En "Development Progress" → "Features in-progress or incomplete":
    Reemplazar la línea `- Avaluos/Contratos: mock UI only, zero backend, no data model` por:
    `- Avaluos/Contratos: mock funcional — UI completa con flujo de captura, cálculos y validaciones; pendiente backend (Contrato/Prestamo/Pago) y confirmación de negocio de 5 decisiones abiertas`

    NO modificar el resto del archivo. Usar Edit (no Write) para mantener el resto intacto.
  </action>
  <verify>
    <automated>cd "C:/Users/Emm-a/Documents/GitHub/prestamil" && grep -c "Avaluos feature is a functional mock" .planning/codebase/CONCERNS.md && grep -c "Avaluos — decisiones pendientes de negocio" .planning/codebase/CONCERNS.md && grep -c "mock funcional — UI completa con flujo de captura" .planning/codebase/CONCERNS.md</automated>
  </verify>
  <done>
    - CONCERNS.md contiene el nuevo bullet "Avaluos feature is a functional mock — UI complete, backend pending"
    - CONCERNS.md contiene la subsección "Avaluos — decisiones pendientes de negocio" con las 5 preguntas
    - CONCERNS.md en Development Progress menciona "mock funcional — UI completa con flujo de captura"
    - El resto del archivo no fue modificado (mismas líneas existentes)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificación visual del mock</name>
  <what-built>
    - Componente `avaluo-mock` reescrito con AuthService real, PlazoService real (con fallback), 4 modales NgbModal (cliente, prenda, contrato, vencimientos), cálculos automáticos de oro por kilataje, validaciones de flujo, tarjeta "Pendiente confirmar" en dev
    - Template Angular con secciones reactivas por tipo (Alhajas/Plata/Varios/Autos-Motos)
    - CONCERNS.md actualizado con nuevo estado del feature y decisiones de negocio pendientes
    - Eliminados: alert(), console.log, mensajeFlujo, clienteDemo hardcodeado
  </what-built>
  <how-to-verify>
    1. Arrancar el frontend: `cd prestamil-frontend && npx ng serve` (asegurarse de que el backend Spring Boot también esté arriba para que PlazoService responda; si no, el fallback estático debe activarse)
    2. Abrir http://localhost:4200/avaluos/mock
    3. **Header**: verificar que "Usuario" muestra el nombre del usuario logueado (no "cajero.demo"). "Rol" muestra "Rol N" donde N es el idRol del usuario. "Fecha abierta" es la fecha actual.
    4. **Mensajes**: la sección de mensaje único naranja desapareció — ahora hay alerts verdes (éxito) / rojos (error) que aparecen solo cuando hay acción
    5. **Plazo**: el `<select>` muestra plazos reales del backend con formato "[nombre] - [N] días". Si no hay backend, debe mostrar los 4 plazosDemo
    6. **Buscar cliente**: clic en "Buscar" → se abre modal con 4 clientes mock. Filtrar por "PEREZ" debe reducir la lista. Clic en "Seleccionar" → cierra modal y llena nombre/folio/identificación
    7. **Captura ALHAJAS**: seleccionar tipo Alhajas. Capturar kilataje 14, peso 5 → debe auto-calcular precioXGramo=695.74, prestamo≈3583.06, avaluoContrato≈5374.59
    8. **Buscar prenda**: clic en "Buscar" del campo Prenda → modal con 8 prendas. Seleccionar "AHD18" → llena clave/descripción/kilataje 18 y recalcula
    9. **Agregar partida sin cliente**: clic en "Agregar partida" sin cliente → alert rojo "Selecciona un cliente..."
    10. **Agregar partida sin plazo**: con cliente seleccionado pero sin plazo → alert rojo "Selecciona un plazo..."
    11. **Agregar partida válida**: con cliente + plazo + kilataje + peso > 0 → se agrega fila a tabla, alert verde "Partida agregada correctamente" (se quita solo a los 3s)
    12. **Captura VARIOS**: seleccionar tipo Varios, capturar préstamo 1500 → avaluoContrato se autocalcula = 2250. Agregar partida funciona igual.
    13. **Captura AUTOS/MOTOS**: seleccionar tipo Autos/Motos → muestra alert "no disponible", botón "Agregar partida" deshabilitado
    14. **Tabla de partidas**: debe tener columna "Avalúo contrato" entre "Avalúo real" y "Préstamo". Resumen rápido debe incluir "Avalúo contrato total"
    15. **Generar contrato**: clic en "Generar contrato" → modal con resumen. "Cancelar" cierra sin tocar nada. "Confirmar y generar" → limpia partidas + cliente, alert verde demo
    16. **Ver vencimientos**: con cliente seleccionado, clic en "Ver vencimientos" → modal con 3 filas mock y título "Vencimientos — [nombre cliente]"
    17. **Tarjeta pendientes**: visible al final solo en dev (`!environment.production`). Botón "Mostrar preguntas" expande las 5 preguntas
    18. **DevTools Console**: NO debe haber console.log del componente (solo logs de AuthService y otros servicios existentes)
    19. **CONCERNS.md**: abrir y confirmar que existe el bullet "Avaluos feature is a functional mock", la subsección "Avaluos — decisiones pendientes de negocio" y el texto actualizado en Development Progress
  </how-to-verify>
  <resume-signal>Type "approved" si el flujo funciona como se describe, o describe issues encontrados (ejemplo: "el cálculo de kilataje 14 me dio X en lugar de Y", o "el modal de cliente no abre")</resume-signal>
</task>

</tasks>

<verification>
- `npx ng build --configuration development` compila sin errores
- Buscar `alert(` en avaluo-mock.component.ts → 0 resultados
- Buscar `console.log` en avaluo-mock.component.ts → 0 resultados
- Buscar `mensajeFlujo` en componente y template → 0 resultados
- Buscar `clienteDemo` en componente y template → 0 resultados
- Buscar `simularAccion`, `simularAgregarPartida`, `simularGenerarContrato` → 0 resultados en todo el proyecto
- CONCERNS.md contiene los 3 cambios especificados en Task 3 (verificado por grep -c en verify automatizado)
- Demo manual del checkpoint pasa los 19 puntos
</verification>

<success_criteria>
- Componente `avaluo-mock` reescrito siguiendo el patrón standalone Angular 20 con `inject()` para servicios
- AuthService.getUser() reemplaza datos de usuario hardcodeados; sucursalId=1 con TODO documentado (alineado a decisión STATE.md 2026-05-16)
- PlazoService.getAll() puebla el `<select>` de plazo, con fallback a `plazosDemo` si la llamada falla o devuelve vacío
- 4 modales NgbModal funcionando: cliente, prenda, contrato, vencimientos — todos como `<ng-template>` + `@ViewChild`
- Cálculos automáticos para ALHAJAS/PLATA siguen la fórmula: precioBase = preciosMockOro[kilataje], prestamo = precioBase × peso × 1.03, avaluoContrato = prestamo × 1.50
- Cálculo automático para VARIOS: avaluoContrato = prestamo × 1.50 (préstamo manual)
- AUTOS/MOTOS muestra mensaje y deshabilita botón Agregar partida
- Validaciones de flujo en agregarPartida(): cliente, plazo, tipo, peso/préstamo > 0
- Patrón successMessage/errorMessage (3s auto-dismiss del success) alineado a empresas.component.ts
- Tabla extendida con columna "Avalúo contrato" + acción eliminar fila
- Resumen rápido incluye "Avalúo contrato total"
- Tarjeta colapsable "Pendiente confirmar" con 5 preguntas, visible solo en dev (`!environment.production`)
- Cero llamadas a `alert()` y `console.log` en el componente final
- CONCERNS.md refleja el nuevo estado del feature en 3 ubicaciones (bullet Incomplete Features, subsección decisiones de negocio, línea de Development Progress)
- Build de Angular en modo development pasa sin errores
- Checkpoint humano: los 19 puntos de verificación visual pasan
</success_criteria>

<output>
After completion, create `.planning/quick/260522-g33-actualizar-mock-de-avaluos-prendarios-re/260522-g33-SUMMARY.md` con:
- Resumen de cambios por archivo
- Decisiones tomadas (e.g., sucursalId hardcodeado a 1 — alineado a STATE.md; rol mostrado como "Rol N" porque AuthService no expone el nombre del rol)
- Issues encontrados durante el checkpoint (si los hubo)
- Pendientes para la próxima iteración (backend Contrato, PDF real, decisiones de negocio del cliente)
</output>
