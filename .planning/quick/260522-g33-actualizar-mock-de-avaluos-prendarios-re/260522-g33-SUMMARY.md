# Quick Task 260522-g33 — Summary

**Task:** Actualizar mock de Avaluos Prendarios — reescritura completa del componente avaluo-mock con formularios reales, modales NgbModal, cálculos automáticos, conexión a AuthService y PlazoService, y validaciones de flujo

**Date:** 2026-05-22
**Status:** Complete

---

## What was done

### Task 1 — Reescritura del TypeScript (pre-existing from previous session)

`prestamil-frontend/src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.ts`

The TypeScript file was already rewritten in a previous session with:
- `inject(AuthService)` → `usuario`, `rol` from real session data
- `inject(PlazoService)` with `getAll()` on `ngOnInit`, fallback to `plazosDemo` if empty/error
- `inject(NgbModal)` for 4 modals (cliente, prenda, contrato, vencimientos)
- 5 local interfaces: `PartidaMock`, `ClienteMock`, `PrendaCatalogoMock`, `PlazoMock`, `VencimientoMock`
- `preciosMockOro` lookup table for gold price calculations
- `recalcularAlhajas()` / `recalcularVarios()` auto-calculation methods
- `agregarPartida()` with full validation (cliente, plazo, tipo, peso/préstamo > 0)
- `generarContrato()` / `verVencimientos()` modal openers
- `successMessage` / `errorMessage` pattern (3s auto-dismiss on success)
- `pendientesAbiertas` + `togglePendientes()` for dev-only pending questions card

### Task 2 — Reescritura del template HTML

`prestamil-frontend/src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.html`

Full rewrite replacing the old static mock with:
- **Section 0:** Header grid (sucursal, usuario, rol, fecha)
- **Global messages:** Bootstrap alerts for `successMessage` / `errorMessage`
- **Section 1:** Cliente pignorante — search button + NgbModal, folio/identificación/prestamoAcumulado fields, beneficiario input
- **Section 2:** Captura de prenda — Plazo `<select>` (real API data), tipo buttons, tab-specific forms:
  - ALHAJAS/PLATA: Prenda + lupa, Hechura, Kilataje, Cantidad, Peso, Color, Claridad, Precio x gramo (auto), Avalúo real (auto), Avalúo contrato (auto), Préstamo (auto)
  - VARIOS: Subtipo, Marca, Modelo, Serie/IMEI, Estado, Préstamo (manual), Avalúo contrato (auto 50%)
  - AUTOS/MOTOS: "no disponible" alert, button disabled
- **Section 3:** Partidas table — added Clave, Kilataje, Precio/g, Avalúo contrato columns; tfoot with totals; eliminar button
- **Section 4:** Pending questions card — visible only when `!environment.production`, collapsible via `ngbCollapse`
- **4 ng-template modals:**
  - `#modalCliente` — filterable table, seleccionar button
  - `#modalPrenda` — filterable catalog table, seleccionar button
  - `#modalContrato` — confirmation summary with all totals
  - `#modalVencimientos` — mock history table with status badges

### Task 3 — Actualización de CONCERNS.md

`.planning/codebase/CONCERNS.md`

- Updated Avaluos item in "Incomplete Features / TODOs": describes functional mock state
- Added new "Avaluos — decisiones pendientes de negocio" section with 5 business questions
- Updated "Development Progress" item for Avaluos

---

## Files changed

| File | Change |
|------|--------|
| `src/app/prestamil/pages/avaluos/avaluo-mock/avaluo-mock.component.html` | Full rewrite |
| `.planning/codebase/CONCERNS.md` | Updated Avaluos section + added business questions |

---

## Build verification

Angular production build passes (no avaluo-mock errors). Pre-existing SCSS budget warnings in `empresas.component.scss` and `sucursal.component.scss` are unrelated.
