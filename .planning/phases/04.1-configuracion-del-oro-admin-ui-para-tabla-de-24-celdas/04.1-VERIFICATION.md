---
phase: 04.1-configuracion-del-oro-admin-ui-para-tabla-de-24-celdas
verified: 2026-07-26T18:23:52Z
status: passed
score: 5/5 must-haves verified (all 3 plans)
---

# Phase 04.1: Configuración del Oro — Admin UI para tabla de 24 celdas Verification Report

**Phase Goal:** El negocio puede editar el %Prestamo de las 24 celdas de oro_tabla_prestamo (8 kilates × 3 hechuras) desde una pantalla dedicada, en vez de solo vía changeset Liquibase — retoma la decisión D-03 (deferred) de Phase 4
**Verified:** 2026-07-26T18:23:52Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `GET /api/oro-tabla-prestamo?sucursalId=1` devuelve 24 celdas con `precioAvaluo`/`precioPrestamo` calculados | ✓ VERIFIED | `OroTablaPrestamoController.getTabla` → `OroTablaPrestamoService.getTabla` computes both fields per cell using the documented COCAE formula (scale 10 HALF_UP intermediate, scale 4 HALF_UP final), sorted by kilataje then hechura |
| 2 | `PUT /api/oro-tabla-prestamo/{kilataje}/{hechura}` persists `%Prestamo` and cascades recalculation into all `PlazoHechuraAlhaja`, preserving `porcAumento` | ✓ VERIFIED | `actualizarCelda` saves the entity then calls `plazoService.recalcularPrecioBasePorTablaOro(sucursalId)`, which reuses the existing private `recalcularRegistros` (does not touch `porcAumento`) |
| 3 | A PUT on kilataje=24 is rejected with HTTP 400 | ✓ VERIFIED | `if (kilataje == 24) throw new BadRequestException(...)` before any repository access |
| 4 | precio_oro schema/entity/DTOs no longer have factor_fundir/normal/especial columns | ✓ VERIFIED | Changeset `013-drop-factores-hechura-precio-oro.sql` drops all 3 columns with explicit rollback, registered in master changelog; `PrecioOro.java`, `PrecioOroRequest.java`, `PrecioOroResponse.java` confirmed to have zero factor fields; zero matches for factor* anywhere in `prestamil-backend/src` |
| 5 | Existe la pantalla Angular `/configuracion/configuracion-oro` con 3 pestañas, 8 kilates, 24K deshabilitado, y campo de precio del gramo | ✓ VERIFIED | `ConfiguracionOroComponent` (standalone) renders `ngbNav` with Fundir/Normal/Especial tabs, table of `KILATES = [6,8,10,12,14,18,21,24]`, 24K row shows `text-muted` + "Referencia (no prendable)" badge with disabled input/button, precio-gramo card with save button wired to `actualizarPrecioGramo` |
| 6 | Ruta registrada, submenú en sidebar, tarjeta legacy "Precio del Oro (global)" retirada de /plazos-periodos | ✓ VERIFIED | Route `configuracion-oro` → `ConfiguracionOroComponent` in `app-routing.module.ts`; `opcionToUrlMap['Configuración del Oro']` set; changeset 014 inserts `opciones.id=16` (idPadre=6) + `roles_opciones` (1,16)/(5,16); zero matches for "Precio del Oro (global)"/`recalcularGlobal`/factor fields in `plazos-periodos.component.html/.ts`/`plazo.model.ts` |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `prestamil-backend/.../changes/013-drop-factores-hechura-precio-oro.sql` | Drops 3 factor columns, rollback | ✓ VERIFIED | Content matches plan exactly; registered in `db.changelog-master.xml` line 20 |
| `prestamil-backend/.../changes/014-menu-configuracion-oro.sql` | Alta submenú + roles_opciones | ✓ VERIFIED | `opciones.id=16`, roles (1,16)/(5,16), rollback present; registered line 21 |
| `prestamil-backend/.../model/PrecioOro.java` | Sin factores | ✓ VERIFIED | Only precioGramo24k/calcularSobre/baseKilataje/actualizadoEn/actualizadoPor remain |
| `prestamil-backend/.../service/PlazoService.java` | `recalcularPrecioBasePorTablaOro` público | ✓ VERIFIED (WIRED) | Method at line 364, exact body from plan; calls `recalcularRegistros(registros, precio.getPrecioGramo24k(), 24, sucursalId)` then `saveAll` |
| `prestamil-backend/.../service/OroTablaPrestamoService.java` | getTabla + actualizarCelda | ✓ VERIFIED (WIRED) | Both methods present, formulas match spec, 24K rejection, cascade call |
| `prestamil-backend/.../controller/OroTablaPrestamoController.java` | GET + PUT endpoints | ✓ VERIFIED (WIRED) | `@RequestMapping("/api/oro-tabla-prestamo")`, GET/PUT present |
| `prestamil-frontend/.../models/oro-config.model.ts` | 4 DTOs, no factor fields | ✓ VERIFIED | `OroCeldaResponse`, `OroCeldaUpdateRequest`, `PrecioGramoResponse`, `PrecioGramoRequest` defined, self-contained |
| `prestamil-frontend/.../services/oro-config.service.ts` | 4 methods | ✓ VERIFIED (WIRED) | `getTablaOro`, `actualizarCelda`, `getPrecioGramo`, `actualizarPrecioGramo` hit `/api/oro-tabla-prestamo` and `/api/precio-oro` |
| `prestamil-frontend/.../configuracion-oro.component.ts/.html/.scss` | 3 tabs, 8 kilates, 24K disabled | ✓ VERIFIED (WIRED) | Component + template match plan; `[disabled]="!c.editable"` on both input and button in 24K row |
| `prestamil-frontend/app-routing.module.ts` | Route registered | ✓ VERIFIED (WIRED) | `path: 'configuracion-oro'` → lazy `loadComponent` → `ConfiguracionOroComponent` |
| `prestamil-frontend/menu-transformer.helper.ts` | Menu mapping | ✓ VERIFIED (WIRED) | `'Configuración del Oro': '/configuracion/configuracion-oro'` present |
| `prestamil-frontend/.../plazos-periodos.component.html/.ts` | Legacy card removed | ✓ VERIFIED | Zero matches for "Precio del Oro (global)", `recalcularGlobal`, `cargarPrecioOro`, factor fields; `recalcularTodo`/`precioBaseOroInput` (per-plazo recalc) preserved (4 matches) |
| `prestamil-frontend/.../models/plazo.model.ts` | PrecioOro types without factors | ✓ VERIFIED | `PrecioOroRequest`/`PrecioOroResponse` retain only non-factor fields |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `OroTablaPrestamoService.actualizarCelda` | `PlazoService.recalcularPrecioBasePorTablaOro` | direct call after save | ✓ WIRED | `plazoService.recalcularPrecioBasePorTablaOro(sucursalId);` present |
| `PlazoService.recalcularPrecioBasePorTablaOro` | `recalcularRegistros` (private) | reuse | ✓ WIRED | Calls existing private method with base=24, does not touch `porcAumento` |
| `configuracion-oro.component.ts` | `/api/oro-tabla-prestamo` | `oro-config.service` | ✓ WIRED | `getTablaOro`/`actualizarCelda` called in `cargarTabla`/`guardarCelda`, results assigned to `celdas` |
| `app-routing.module.ts` | `ConfiguracionOroComponent` | lazy `loadComponent` | ✓ WIRED | Confirmed at lines 97-100 |
| `menu-transformer.helper.ts` | `/configuracion/configuracion-oro` | `opcionToUrlMap` entry | ✓ WIRED | Confirmed at line 86, matches changeset 014's `opciones.opcion` string exactly |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `ConfiguracionOroComponent.celdas` | `celdas: OroCeldaResponse[]` | `oroConfigService.getTablaOro()` → `GET /api/oro-tabla-prestamo` → `OroTablaPrestamoService.getTabla` → `oroTablaPrestamoRepository.findByIdSucursalId` (real DB query against `oro_tabla_prestamo`, populated by Phase 4's changeset 012 with 24 COCAE cells) | Yes | ✓ FLOWING |
| `ConfiguracionOroComponent.precioGramo` | `precioGramo: number \| null` | `oroConfigService.getPrecioGramo()` → `GET /api/precio-oro` (pre-existing `PrecioOroController`, not modified by this phase except DTO field removal) | Yes | ✓ FLOWING |

No hardcoded empty returns or disconnected props found; both GET endpoints perform real repository queries.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Backend full test suite (regression + new code paths) | `mvn -o test` (run directly, not via `mvnw` which failed offline) | `Tests run: 35, Failures: 0, Errors: 0` — `BUILD SUCCESS` | ✓ PASS |
| `PlazoServiceTest` reflects planned edits | `grep -n "buildPrecioOro\|@Test"` | 3 `@Test` methods present; `actualizarTodosPrecios_ignoraFactoresHechuraGlobales` removed; `buildPrecioOro(Integer baseKilataje)` single-param signature confirmed | ✓ PASS |
| Frontend TypeScript compiles clean | `npx tsc --noEmit -p tsconfig.app.json` | Exit code 0, no output | ✓ PASS |
| Live HTTP call to new endpoints | N/A | Not run — requires running MariaDB + Spring Boot server | ? SKIP (no runnable entry point started; routed to human verification) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| ORO-05 | 04.1-01, 04.1-02 | Negocio edita %Prestamo de las 24 celdas desde pantalla nueva | ✓ SATISFIED | Backend GET/PUT endpoints + frontend screen both verified wired end-to-end |
| ORO-06 | 04.1-01 | Editar %Prestamo dispara cascada preservando porcAumento | ✓ SATISFIED | `recalcularPrecioBasePorTablaOro` reuses `recalcularRegistros`, which per Phase 4 code does not modify `porcAumento` |
| ORO-07 | 04.1-01, 04.1-02 | 24K se muestra como referencia no editable | ✓ SATISFIED | Backend rejects PUT on kilataje=24 (400); frontend disables input/button and shows badge |
| ORO-08 | 04.1-01, 04.1-03 | Factores de hechura eliminados de UI, DTOs y schema | ✓ SATISFIED | Zero references to factorFundir/Normal/Especial across backend src and frontend src; changeset 013 drops the columns with rollback |

No orphaned requirements — all 4 IDs traced in REQUIREMENTS.md map to Phase 4.1 and are claimed across the 3 plans exactly as declared.

### Anti-Patterns Found

None found. No TODO/FIXME/placeholder markers, no empty stub implementations, no hardcoded-empty return values in any of the 15 files touched by this phase's 3 plans.

### Human Verification Required

### 1. Visual/UX check of the "Configuración del Oro" screen

**Test:** Log in as a user with role Sistemas(1) or Gerente(5), navigate to Configuración → Configuración del Oro, confirm the 3 tabs render, edit a non-24K cell's %Prestamo, save, and confirm the success toast and updated Precio Prestamo column value appear.
**Expected:** Tabs switch correctly, edited value persists after reload, 24K row visibly greyed out with badge and disabled controls in all 3 tabs.
**Why human:** Visual rendering, tab-switch interaction, and toast timing cannot be verified via static code analysis alone.

### 2. End-to-end cascade recalculation against a real DB

**Test:** With the backend running against the real MariaDB instance, edit a cell's %Prestamo via the UI (or `curl`), then query `PlazoHechuraAlhaja` rows for that kilataje/hechura across all existing plazos to confirm `precioBase` updated and `porcAumento` unchanged.
**Expected:** Only `precioBase` (and downstream `precioPrestamo`) change; `porcAumento` values are byte-for-byte identical to before the edit.
**Why human:** Requires a live database with pre-existing `PlazoHechuraAlhaja` data; not verifiable via source-code inspection alone (the unit test in `PlazoServiceTest` covers the formula, but not a live multi-plazo cascade against production-shaped data).

### 3. Sidebar menu visibility per role

**Test:** Log in as a role other than Sistemas(1)/Gerente(5) and confirm "Configuración del Oro" does NOT appear under Configuración; log in as Sistemas or Gerente and confirm it does.
**Expected:** Menu item visibility follows `roles_opciones` exactly as inserted by changeset 014.
**Why human:** Requires querying the live `SPRING_SESSION`-backed menu endpoint (`/api/usuarios/me`) with real role-bound credentials.

### Gaps Summary

No gaps found. All 4 requirement IDs (ORO-05, ORO-06, ORO-07, ORO-08) are satisfied by code that exists, is substantive, is wired end-to-end, and (where applicable) traces to real data sources rather than stubs. Backend compiles and the full test suite passes (35/35, verified independently in this session via `mvn -o test`, not just trusted from the SUMMARY.md claim). Frontend compiles cleanly (`tsc --noEmit` exit 0, verified independently). All three git commits referenced in the phase brief (`4b6c770`, `fcfd411`, `ccf4c8c` in prestamil-backend; `789b467`, `951c68a`, `7674db9` in prestamil-frontend) were confirmed present via `git log`. The three human-verification items above are standard UI/live-DB/live-session checks that cannot be substituted by static analysis and do not block the "passed" determination — they are recommended follow-up manual smoke tests before considering the feature production-ready.

---

*Verified: 2026-07-26T18:23:52Z*
*Verifier: Claude (gsd-verifier)*
