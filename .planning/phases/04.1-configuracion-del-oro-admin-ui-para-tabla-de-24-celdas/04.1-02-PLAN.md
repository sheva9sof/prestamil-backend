---
phase: 04.1-configuracion-del-oro-admin-ui-para-tabla-de-24-celdas
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - prestamil-frontend/src/app/prestamil/core/models/oro-config.model.ts
  - prestamil-frontend/src/app/prestamil/core/services/oro-config.service.ts
  - prestamil-frontend/src/app/prestamil/pages/configuracion/configuracion-oro/configuracion-oro.component.ts
  - prestamil-frontend/src/app/prestamil/pages/configuracion/configuracion-oro/configuracion-oro.component.html
  - prestamil-frontend/src/app/prestamil/pages/configuracion/configuracion-oro/configuracion-oro.component.scss
  - prestamil-frontend/src/app/app-routing.module.ts
  - prestamil-frontend/src/app/prestamil/core/helpers/menu-transformer.helper.ts
  - prestamil-backend/src/main/resources/db/changelog/changes/014-menu-configuracion-oro.sql
  - prestamil-backend/src/main/resources/db/changelog/db.changelog-master.xml
autonomous: true
requirements: [ORO-05, ORO-07]

must_haves:
  truths:
    - "Existe la ruta /configuracion/configuracion-oro que carga la pantalla Configuración del Oro"
    - "La pantalla muestra 3 pestañas (Fundir/Normal/Especial) cada una con una tabla de 8 kilates (6,8,10,12,14,18,21,24K)"
    - "Cada fila muestra Precio Avalúo (solo lectura), % Prestamo (editable salvo 24K) y Precio Prestamo de referencia (solo lectura)"
    - "La fila 24K tiene el input de %Prestamo deshabilitado"
    - "Existe un campo Precio del gramo de oro 24K que al guardar llama a PUT /api/precio-oro"
    - "Aparece un submenú 'Configuración del Oro' bajo Configuración en el sidebar"
  artifacts:
    - path: "prestamil-frontend/src/app/prestamil/pages/configuracion/configuracion-oro/configuracion-oro.component.ts"
      provides: "Componente standalone con las 3 pestañas y edición de %Prestamo"
      min_lines: 60
    - path: "prestamil-frontend/src/app/prestamil/core/services/oro-config.service.ts"
      provides: "getTablaOro, actualizarCelda, getPrecioGramo, actualizarPrecioGramo"
    - path: "prestamil-backend/src/main/resources/db/changelog/changes/014-menu-configuracion-oro.sql"
      provides: "Alta del submenú en opciones + roles_opciones"
      contains: "Configuración del Oro"
  key_links:
    - from: "configuracion-oro.component.ts"
      to: "/api/oro-tabla-prestamo"
      via: "oro-config.service.getTablaOro / actualizarCelda"
      pattern: "oro-tabla-prestamo"
    - from: "app-routing.module.ts"
      to: "configuracion-oro.component"
      via: "loadComponent lazy route bajo configuracion"
      pattern: "configuracion-oro"
    - from: "menu-transformer.helper.ts"
      to: "/configuracion/configuracion-oro"
      via: "entrada en opcionToUrlMap"
      pattern: "Configuración del Oro"
---

<objective>
Pantalla Angular "Configuración del Oro" bajo el menú Configuración: 3 pestañas (Fundir/Normal/Especial), tabla de 8 kilates con Precio Avalúo (RO), % Prestamo (editable, 24K deshabilitado) y Precio Prestamo de referencia (RO), más el campo de precio del gramo de oro que se muda desde /plazos-periodos (D-16). Incluye ruta, mapeo de menú y changeset de alta en la tabla `opciones`.

Purpose: Dar al negocio una pantalla dedicada para editar el %Prestamo global de las 24 celdas (ORO-05) con la regla 24K de referencia no editable (ORO-07).
Output: Modelo, servicio y componente nuevos, ruta registrada, mapeo de menú y changeset 014.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.1-configuracion-del-oro-admin-ui-para-tabla-de-24-celdas/04.1-CONTEXT.md
@.planning/phases/04.1-configuracion-del-oro-admin-ui-para-tabla-de-24-celdas/04.1-01-SUMMARY.md

<interfaces>
<!-- Contratos backend expuestos por Plan 01 (04.1-01). El componente los consume directamente. -->

GET  /api/oro-tabla-prestamo?sucursalId=1  -> OroCeldaResponse[]  (24 celdas)
PUT  /api/oro-tabla-prestamo/{kilataje}/{hechura}?sucursalId=1  body: { porcPrestamo: number }  -> OroCeldaResponse
GET  /api/precio-oro?sucursalId=1  -> { id, sucursalId, precioGramo24k, calcularSobre, baseKilataje, actualizadoEn, actualizadoPor }  (SIN factores tras Plan 01)
PUT  /api/precio-oro?sucursalId=1  body: { precioGramoBase: number }  -> mismo shape (recalcula todas las tablas)

OroCeldaResponse (TS a definir en oro-config.model.ts):
```typescript
export interface OroCeldaResponse {
  kilataje: number;
  hechura: string;            // 'F' | 'N' | 'E'
  hechuraDescripcion: string; // 'Fundir' | 'Normal' | 'Especial'
  precioAvaluo: number;
  porcPrestamo: number;
  precioPrestamo: number;
  editable: boolean;          // false para 24K
}
```
</interfaces>

<!-- Patrones de referencia (leer, no modificar): -->
<!-- Componente standalone de configuración: prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.ts (uso de SharedModule, NgbNavModule para pestañas, inject(), app-card) -->
<!-- Ruta hija de configuracion: prestamil-frontend/src/app/app-routing.module.ts líneas 74-101 -->
<!-- Mapeo de menú: prestamil-frontend/src/app/prestamil/core/helpers/menu-transformer.helper.ts opcionToUrlMap líneas 68-87 -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modelo + servicio de configuración del oro</name>
  <files>prestamil-frontend/src/app/prestamil/core/models/oro-config.model.ts, prestamil-frontend/src/app/prestamil/core/services/oro-config.service.ts</files>
  <read_first>
    - prestamil-frontend/src/app/prestamil/core/services/plazo.service.ts (patrón de servicio: inject(HttpClient), environment.apiUrl, params sucursalId)
    - prestamil-frontend/src/environments/environment.ts (apiUrl)
    - .planning/phases/04.1-configuracion-del-oro-admin-ui-para-tabla-de-24-celdas/04.1-01-SUMMARY.md (contratos backend finales)
  </read_first>
  <action>
    1. Crear `prestamil-frontend/src/app/prestamil/core/models/oro-config.model.ts` con:
       - `OroCeldaResponse` (ver `<interfaces>` arriba, exacto).
       - `OroCeldaUpdateRequest { porcPrestamo: number; }`
       - `PrecioGramoResponse { id: number; sucursalId: number; precioGramo24k: number; calcularSobre: string; baseKilataje: number; actualizadoEn?: string; actualizadoPor?: string; }`  (SIN campos factor).
       - `PrecioGramoRequest { precioGramoBase: number; }`
       Nota: definir estos tipos aquí (no importar de plazo.model.ts) para mantener el módulo autocontenido; plazo.model.ts se limpia en Plan 03.
    2. Crear `prestamil-frontend/src/app/prestamil/core/services/oro-config.service.ts` — `@Injectable({ providedIn: 'root' })`, `inject(HttpClient)`, con:
       - `private readonly API_ORO = ${environment.apiUrl}/api/oro-tabla-prestamo;`
       - `private readonly API_PRECIO = ${environment.apiUrl}/api/precio-oro;`
       - `getTablaOro(sucursalId = 1): Observable<OroCeldaResponse[]>` -> GET API_ORO con `{ params: { sucursalId: String(sucursalId) } }`.
       - `actualizarCelda(kilataje: number, hechura: string, porcPrestamo: number, sucursalId = 1): Observable<OroCeldaResponse>` -> PUT `${API_ORO}/${kilataje}/${hechura}` body `{ porcPrestamo }` con params sucursalId.
       - `getPrecioGramo(sucursalId = 1): Observable<PrecioGramoResponse>` -> GET API_PRECIO con params sucursalId.
       - `actualizarPrecioGramo(precioGramoBase: number, sucursalId = 1): Observable<PrecioGramoResponse>` -> PUT API_PRECIO body `{ precioGramoBase }` con params sucursalId.
  </action>
  <verify>
    <automated>cd prestamil-frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface OroCeldaResponse" prestamil-frontend/src/app/prestamil/core/models/oro-config.model.ts` devuelve 1
    - `grep -c "getTablaOro\|actualizarCelda\|getPrecioGramo\|actualizarPrecioGramo" prestamil-frontend/src/app/prestamil/core/services/oro-config.service.ts` es >= 4
    - `grep -c "oro-tabla-prestamo" prestamil-frontend/src/app/prestamil/core/services/oro-config.service.ts` es >= 1
    - `grep -c "factorFundir\|factorNormal\|factorEspecial" prestamil-frontend/src/app/prestamil/core/models/oro-config.model.ts` devuelve 0
    - `npx tsc --noEmit` no reporta errores en los archivos nuevos
  </acceptance_criteria>
  <done>Modelo y servicio compilan y exponen los 4 métodos; sin campos factor.</done>
</task>

<task type="auto">
  <name>Task 2: Componente standalone Configuración del Oro (3 pestañas, 8 kilates, 24K disabled)</name>
  <files>prestamil-frontend/src/app/prestamil/pages/configuracion/configuracion-oro/configuracion-oro.component.ts, prestamil-frontend/src/app/prestamil/pages/configuracion/configuracion-oro/configuracion-oro.component.html, prestamil-frontend/src/app/prestamil/pages/configuracion/configuracion-oro/configuracion-oro.component.scss</files>
  <read_first>
    - prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.ts (patrón: selector app-*, standalone, imports [CommonModule, SharedModule, FormsModule, NgbNavModule], inject, manejo successMessage/errorMessage)
    - prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.html (uso de <app-card>, tablas, [(ngModel)])
    - prestamil-frontend/src/app/theme/shared/shared.module.ts (qué re-exporta SharedModule)
    - prestamil-frontend/src/app/prestamil/core/models/oro-config.model.ts (creado en Task 1)
    - prestamil-frontend/src/app/prestamil/core/services/oro-config.service.ts (creado en Task 1)
  </read_first>
  <action>
    Crear el componente en `prestamil-frontend/src/app/prestamil/pages/configuracion/configuracion-oro/` (`.ts`, `.html`, `.scss`).

    `configuracion-oro.component.ts`:
    - `@Component({ selector: 'app-configuracion-oro', standalone: true, imports: [CommonModule, SharedModule, FormsModule, NgbNavModule], templateUrl, styleUrls })`. Clase `ConfiguracionOroComponent implements OnInit`.
    - `inject(OroConfigService)`. Estado: `sucursalId = 1;` `celdas: OroCeldaResponse[] = [];` `precioGramo: number | null = null;` `activeTab = 'F';` `isLoading = false;` `isSavingGramo = false;` `successMessage = '';` `errorMessage = '';` `editando: { [key: string]: number | null } = {};` `savingCelda: { [key: string]: boolean } = {};`
    - Constante de orden de kilates: `readonly KILATES = [6, 8, 10, 12, 14, 18, 21, 24];` y `readonly HECHURAS = [{ code: 'F', label: 'Fundir' }, { code: 'N', label: 'Normal' }, { code: 'E', label: 'Especial' }];`
    - `ngOnInit()`: llama `cargarTabla()` y `cargarPrecioGramo()`.
    - `cargarTabla()`: `oroConfigService.getTablaOro(sucursalId)` -> asigna `celdas`.
    - `cargarPrecioGramo()`: `getPrecioGramo(sucursalId)` -> `precioGramo = data.precioGramo24k` (tolerar error si no hay precio configurado, dejando null).
    - `celdasDeHechura(code: string): OroCeldaResponse[]`: filtra `celdas` por `hechura === code` ordenadas según `KILATES`.
    - `keyCelda(c): string` -> `${c.kilataje}-${c.hechura}`.
    - `iniciarEdicion(c)`: sólo si `c.editable`; setea `editando[key] = c.porcPrestamo`.
    - `guardarCelda(c)`: lee `editando[key]`, valida numérico; llama `actualizarCelda(c.kilataje, c.hechura, valor, sucursalId)`; en next reemplaza la celda en `celdas`, limpia `editando[key]`, `successMessage = 'Porcentaje actualizado. Tablas de préstamo recalculadas.'`; en error `errorMessage`.
    - `cancelarEdicion(c)`: borra `editando[key]`.
    - `guardarPrecioGramo()`: valida `precioGramo > 0`; `actualizarPrecioGramo(precioGramo, sucursalId)` -> en next recarga la tabla (`cargarTabla()`) y `successMessage = 'Precio del gramo guardado. Todas las tablas fueron recalculadas.'`.
    - `trackByCelda(_i, c)` -> `keyCelda(c)`.

    `configuracion-oro.component.html`:
    - Un `<app-card [hidHeader]="true">` de encabezado con título "Configuración del Oro" y el campo "Precio del gramo de oro 24K ($/gramo MXN)": un `<input type="number" [(ngModel)]="precioGramo" [ngModelOptions]="{ standalone: true }" min="0" step="0.01">` y un botón "Guardar y recalcular" que llama `guardarPrecioGramo()` (disabled si `isSavingGramo || !precioGramo`).
    - Un `<app-card [hidHeader]="true">` con `ngbNav` (NgbNavModule): 3 `ngbNavItem` con labels Fundir/Normal/Especial vinculados a `activeTab` = 'F'/'N'/'E'. Cada panel renderiza una tabla con columnas: Kilataje, Precio Avalúo (solo lectura, `{{ c.precioAvaluo | number:'1.2-2' }}`), % Prestamo (input editable), Precio Prestamo (solo lectura, `{{ c.precioPrestamo | number:'1.2-2' }}`).
    - Para % Prestamo: si `editando[keyCelda(c)]` está definido, mostrar `<input type="number" [(ngModel)]="editando[keyCelda(c)]" [disabled]="!c.editable">` + botones guardar/cancelar; si no, mostrar el valor `{{ c.porcPrestamo }}%` con botón "Editar" `[disabled]="!c.editable"` que llama `iniciarEdicion(c)`.
    - La fila 24K debe verse deshabilitada: aplicar clase `text-muted` cuando `!c.editable` y el botón Editar / input deben estar `[disabled]="!c.editable"` (ORO-07/D-12). Mostrar un badge "Referencia (no prendable)" en la fila 24K.
    - Bloques de `successMessage` / `errorMessage` (patrón alert de plazos-periodos).

    `configuracion-oro.component.scss`: estilos mínimos (puede quedar prácticamente vacío o con reglas menores para la tabla).

    NO incluir: pestaña de precio de venta, parámetros del sistema, calibración de centenario (D-18 — fuera de alcance).
  </action>
  <verify>
    <automated>cd prestamil-frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Existen los 3 archivos del componente en `pages/configuracion/configuracion-oro/`
    - `grep -c "selector: 'app-configuracion-oro'" prestamil-frontend/src/app/prestamil/pages/configuracion/configuracion-oro/configuracion-oro.component.ts` devuelve 1
    - `grep -c "\[6, 8, 10, 12, 14, 18, 21, 24\]" prestamil-frontend/src/app/prestamil/pages/configuracion/configuracion-oro/configuracion-oro.component.ts` devuelve 1
    - `grep -c "c.editable\|editable" prestamil-frontend/src/app/prestamil/pages/configuracion/configuracion-oro/configuracion-oro.component.html` es >= 1 (deshabilitación 24K)
    - `grep -c "guardarPrecioGramo\|actualizarPrecioGramo" prestamil-frontend/src/app/prestamil/pages/configuracion/configuracion-oro/configuracion-oro.component.ts` es >= 1
    - `grep -ci "precio.*venta\|centenario\|parametros del sistema" prestamil-frontend/src/app/prestamil/pages/configuracion/configuracion-oro/configuracion-oro.component.html` devuelve 0 (D-18)
    - `npx tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>El componente muestra 3 pestañas con 8 kilates, edita %Prestamo (24K disabled) y guarda el precio del gramo; compila.</done>
</task>

<task type="auto">
  <name>Task 3: Ruta + mapeo de menú + changeset 014 de alta en opciones</name>
  <files>prestamil-frontend/src/app/app-routing.module.ts, prestamil-frontend/src/app/prestamil/core/helpers/menu-transformer.helper.ts, prestamil-backend/src/main/resources/db/changelog/changes/014-menu-configuracion-oro.sql, prestamil-backend/src/main/resources/db/changelog/db.changelog-master.xml</files>
  <read_first>
    - prestamil-frontend/src/app/app-routing.module.ts (rutas hijas de 'configuracion', líneas 74-101)
    - prestamil-frontend/src/app/prestamil/core/helpers/menu-transformer.helper.ts (opcionToUrlMap líneas 68-87)
    - prestamil-backend/src/main/resources/db/changelog/changes/008-menu-avaluos.sql (patrón de INSERT en opciones + roles_opciones)
    - prestamil-backend/src/main/resources/db/changelog/db.changelog-master.xml (registro de includes; 013 ya registrado por Plan 01)
  </read_first>
  <action>
    1. En `app-routing.module.ts`, dentro del bloque `path: 'configuracion' > children`, agregar (junto a las demás rutas de configuración):
```typescript
{
  path: 'configuracion-oro',
  loadComponent: () => import('./prestamil/pages/configuracion/configuracion-oro/configuracion-oro.component').then((c) => c.ConfiguracionOroComponent)
},
```
    2. En `menu-transformer.helper.ts`, agregar dentro del objeto `opcionToUrlMap` (después de la línea `'Plazos y Periodos': '/configuracion/plazos-periodos',`):
```typescript
'Configuración del Oro': '/configuracion/configuracion-oro',
```
    3. Crear `prestamil-backend/src/main/resources/db/changelog/changes/014-menu-configuracion-oro.sql` con el siguiente contenido exacto (id de opción 16 — verificado: el máximo actual es 15/Avalúos; submenú de Configuración -> idPadre=6, principalMenu=0; asignado a los mismos roles 1 y 5 que los otros submenús de Configuración):
```sql
--liquibase formatted sql

--changeset emm-a:014-1
--comment: Alta del submenu "Configuracion del Oro" bajo el menu Configuracion (idPadre=6).
INSERT INTO `opciones` (`id`, `opcion`, `estatus`, `principalMenu`, `permiso`, `idPadre`, `icono`, `nombreIcono`) VALUES
(16, 'Configuración del Oro', 1, 0, 0, 6, 0, NULL);
--rollback DELETE FROM `opciones` WHERE `id` = 16;

--changeset emm-a:014-2
--comment: Asignar el submenu a los roles que ya ven Configuracion (Sistemas=1, Gerente=5).
INSERT INTO `roles_opciones` (`idRol`, `idOpcion`) VALUES
(1, 16),
(5, 16);
--rollback DELETE FROM `roles_opciones` WHERE `idOpcion` = 16;
```
    4. Registrar en `db.changelog-master.xml`: agregar `<include file="db/changelog/changes/014-menu-configuracion-oro.sql"/>` después de la línea del include de `013-drop-factores-hechura-precio-oro.sql` (agregado por Plan 01).
  </action>
  <verify>
    <automated>cd prestamil-frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "configuracion-oro" prestamil-frontend/src/app/app-routing.module.ts` es >= 1
    - `grep -c "ConfiguracionOroComponent" prestamil-frontend/src/app/app-routing.module.ts` devuelve 1
    - `grep -c "Configuración del Oro" prestamil-frontend/src/app/prestamil/core/helpers/menu-transformer.helper.ts` devuelve 1
    - `grep -c "16, 'Configuración del Oro'" prestamil-backend/src/main/resources/db/changelog/changes/014-menu-configuracion-oro.sql` devuelve 1
    - `grep -c "014-menu-configuracion-oro.sql" prestamil-backend/src/main/resources/db/changelog/db.changelog-master.xml` devuelve 1
    - `grep -c "(1, 16)\|(5, 16)" prestamil-backend/src/main/resources/db/changelog/changes/014-menu-configuracion-oro.sql` es >= 2
    - `npx tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>La ruta carga el componente, el menú mapea la nueva opción y el changeset 014 da de alta el submenú para los roles correctos.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` termina sin errores en el frontend.
- Navegar a /configuracion/configuracion-oro carga la pantalla; el sidebar muestra "Configuración del Oro" bajo Configuración.
- La fila 24K aparece deshabilitada; editar una celda prendable guarda y recalcula.
</verification>

<success_criteria>
- ORO-05: el negocio edita el %Prestamo de las 24 celdas desde la pantalla nueva.
- ORO-07: la fila 24K se muestra como referencia no editable en el frontend.
- El campo de precio del gramo de oro vive ahora en esta pantalla (D-16).
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-configuracion-del-oro-admin-ui-para-tabla-de-24-celdas/04.1-02-SUMMARY.md`
</output>
