---
phase: 04.1-configuracion-del-oro-admin-ui-para-tabla-de-24-celdas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prestamil-backend/src/main/resources/db/changelog/changes/013-drop-factores-hechura-precio-oro.sql
  - prestamil-backend/src/main/resources/db/changelog/db.changelog-master.xml
  - prestamil-backend/src/main/java/com/ignis/prestamil/model/PrecioOro.java
  - prestamil-backend/src/main/java/com/ignis/prestamil/request/PrecioOroRequest.java
  - prestamil-backend/src/main/java/com/ignis/prestamil/response/PrecioOroResponse.java
  - prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java
  - prestamil-backend/src/test/java/com/ignis/prestamil/service/PlazoServiceTest.java
  - prestamil-backend/src/main/java/com/ignis/prestamil/response/OroCeldaResponse.java
  - prestamil-backend/src/main/java/com/ignis/prestamil/request/OroCeldaUpdateRequest.java
  - prestamil-backend/src/main/java/com/ignis/prestamil/service/OroTablaPrestamoService.java
  - prestamil-backend/src/main/java/com/ignis/prestamil/controller/OroTablaPrestamoController.java
autonomous: true
requirements: [ORO-05, ORO-06, ORO-07, ORO-08]

must_haves:
  truths:
    - "GET /api/oro-tabla-prestamo?sucursalId=1 devuelve 24 celdas (8 kilates x 3 hechuras) con precioAvaluo y precioPrestamo calculados"
    - "PUT /api/oro-tabla-prestamo/{kilataje}/{hechura} con porcPrestamo persiste el cambio y recalcula precioBase en todos los PlazoHechuraAlhaja preservando porcAumento"
    - "Un PUT sobre kilataje=24 es rechazado con HTTP 400 (BadRequestException)"
    - "El schema precio_oro ya no tiene columnas factor_fundir/factor_normal/factor_especial y los DTOs/entidad tampoco"
    - "Las fuentes de test (src/test) siguen compilando tras eliminar los factores (test-compile en BUILD SUCCESS)"
  artifacts:
    - path: "prestamil-backend/src/main/resources/db/changelog/changes/013-drop-factores-hechura-precio-oro.sql"
      provides: "Changeset que elimina las 3 columnas factor de precio_oro"
      contains: "DROP COLUMN factor_fundir"
    - path: "prestamil-backend/src/main/java/com/ignis/prestamil/service/OroTablaPrestamoService.java"
      provides: "getTabla (24 celdas computadas) + actualizarCelda (persiste %Prestamo, rechaza 24K, dispara cascada)"
    - path: "prestamil-backend/src/main/java/com/ignis/prestamil/controller/OroTablaPrestamoController.java"
      provides: "Endpoints GET lista + PUT celda"
      exports: ["GET", "PUT"]
  key_links:
    - from: "OroTablaPrestamoService.actualizarCelda"
      to: "PlazoService.recalcularPrecioBasePorTablaOro"
      via: "llamada de cascada tras persistir %Prestamo"
      pattern: "recalcularPrecioBasePorTablaOro"
    - from: "PlazoService.recalcularPrecioBasePorTablaOro"
      to: "recalcularRegistros"
      via: "reutiliza el método privado existente"
      pattern: "recalcularRegistros\\("
---

<objective>
Backend completo para la pantalla "Configuración del Oro": endpoints para leer las 24 celdas de `oro_tabla_prestamo` con sus precios calculados y para editar el `%Prestamo` de una celda con recálculo en cascada, más la eliminación del código muerto de los 3 factores de hechura de `PrecioOro` (ORO-08).

Purpose: Habilitar que el negocio edite la tabla de %Prestamo desde una pantalla (hoy solo se puebla vía Liquibase) y limpiar la deuda técnica de los factores sin efecto desde Phase 4.
Output: Changeset 013, entidad/DTOs de PrecioOro sin factores, nuevos DTOs OroCelda, OroTablaPrestamoService, OroTablaPrestamoController, y un método público de cascada en PlazoService.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-configuracion-del-oro-admin-ui-para-tabla-de-24-celdas/04.1-CONTEXT.md

<interfaces>
<!-- Contratos clave que el executor debe respetar. Extraídos del codebase. -->

Entidad clave y su clave compuesta (model/OroTablaPrestamo.java, model/OroTablaPrestamoId.java):
```java
// OroTablaPrestamo: @EmbeddedId OroTablaPrestamoId id; BigDecimal porcPrestamo; LocalDateTime actualizadoEn;
// OroTablaPrestamoId: Integer sucursalId; Integer kilataje; String hechura ("F"|"N"|"E");
```

Repositorio existente (repository/OroTablaPrestamoRepository.java):
```java
List<OroTablaPrestamo> findByIdSucursalId(Integer sucursalId); // ya existe
```

PrecioOro existente (model/PrecioOro.java) — campos que SE CONSERVAN: precioGramo24k, calcularSobre, baseKilataje.
Repositorio: precioOroRepository.findBySucursalId(Integer) -> Optional<PrecioOro>.

Método privado a REUTILIZAR (service/PlazoService.java, líneas ~325-354):
```java
private void recalcularRegistros(List<PlazoHechuraAlhaja> registros,
        BigDecimal precioGramoBase, int baseKilataje, Integer sucursalId)
// Aplica formula COCAE: precioAvaluo = (precioGramoBase/baseKilataje)*kilataje;
// precioBase = precioAvaluo * porcPrestamo(celda)/100 (desde oro_tabla_prestamo);
// precioPrestamo = precioBase * (1 + porcAumento_de_la_fila/100). NO toca porcAumento.
```

Repositorio de registros (ya inyectado en PlazoService):
```java
plazoHechuraAlhajaRepository.findByIdSucursalId(Integer sucursalId) -> List<PlazoHechuraAlhaja>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminar factores de hechura de precio_oro (ORO-08)</name>
  <files>prestamil-backend/src/main/resources/db/changelog/changes/013-drop-factores-hechura-precio-oro.sql, prestamil-backend/src/main/resources/db/changelog/db.changelog-master.xml, prestamil-backend/src/main/java/com/ignis/prestamil/model/PrecioOro.java, prestamil-backend/src/main/java/com/ignis/prestamil/request/PrecioOroRequest.java, prestamil-backend/src/main/java/com/ignis/prestamil/response/PrecioOroResponse.java, prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java, prestamil-backend/src/test/java/com/ignis/prestamil/service/PlazoServiceTest.java</files>
  <read_first>
    - prestamil-backend/src/main/resources/db/changelog/changes/011-oro-sancion-plata.sql (changeset 011-8 que agregó las columnas factor)
    - prestamil-backend/src/main/resources/db/changelog/db.changelog-master.xml (patrón de registro de includes)
    - prestamil-backend/src/main/java/com/ignis/prestamil/model/PrecioOro.java
    - prestamil-backend/src/main/java/com/ignis/prestamil/request/PrecioOroRequest.java
    - prestamil-backend/src/main/java/com/ignis/prestamil/response/PrecioOroResponse.java
    - prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java (métodos recalcularTodasLasTablas, toPrecioOroResponse, primerNoNulo)
    - prestamil-backend/src/test/java/com/ignis/prestamil/service/PlazoServiceTest.java (helper buildPrecioOro y sus call sites; test actualizarTodosPrecios_ignoraFactoresHechuraGlobales que valida el comportamiento ahora eliminado)
  </read_first>
  <action>
    1. Crear `013-drop-factores-hechura-precio-oro.sql` en `prestamil-backend/src/main/resources/db/changelog/changes/` con este contenido exacto (formato liquibase SQL, autor `emm-a`, con rollback explícito):
```sql
--liquibase formatted sql

--changeset emm-a:013-1
--comment: Eliminar factores de hechura (factor_fundir/normal/especial) de precio_oro.
--          Codigo muerto desde Phase 4: recalcularRegistros deriva precio_base de oro_tabla_prestamo,
--          no de estos factores (D-17). Se elimina para no mantener dos fuentes de precio.
ALTER TABLE precio_oro
  DROP COLUMN factor_fundir,
  DROP COLUMN factor_normal,
  DROP COLUMN factor_especial;
--rollback ALTER TABLE precio_oro ADD COLUMN factor_fundir DECIMAL(7,4) NOT NULL DEFAULT 90.0000 AFTER base_kilataje, ADD COLUMN factor_normal DECIMAL(7,4) NOT NULL DEFAULT 100.0000 AFTER factor_fundir, ADD COLUMN factor_especial DECIMAL(7,4) NOT NULL DEFAULT 110.0000 AFTER factor_normal;
```
    2. Registrar el changeset en `db.changelog-master.xml` agregando `<include file="db/changelog/changes/013-drop-factores-hechura-precio-oro.sql"/>` inmediatamente después de la línea de `012-oro-tabla-prestamo-cocae.sql`.
    3. En `PrecioOro.java`: eliminar los 3 campos `factorFundir`, `factorNormal`, `factorEspecial` con sus anotaciones `@Column` (líneas ~38-48) y sus comentarios Javadoc. Conservar precioGramo24k, calcularSobre, baseKilataje, actualizadoEn, actualizadoPor.
    4. En `PrecioOroRequest.java`: eliminar los 3 campos `factorFundir`, `factorNormal`, `factorEspecial` (líneas ~24-31). Conservar precioGramoBase, baseKilataje, calcularSobre.
    5. En `PrecioOroResponse.java`: eliminar los 3 campos `factorFundir`, `factorNormal`, `factorEspecial` (líneas ~17-19).
    6. En `PlazoService.java`, método `recalcularTodasLasTablas`: eliminar las 3 líneas que resuelven `factorFundir`/`factorNormal`/`factorEspecial` con `primerNoNulo(...)`, eliminar las 3 llamadas `precio.setFactorFundir/Normal/Especial(...)`, y quitar los factores del `log.info` (dejar el log con sucursal, precioGramo, base y registros). En `toPrecioOroResponse`: eliminar las 3 líneas `r.setFactorFundir/Normal/Especial(...)`. Eliminar el método privado `primerNoNulo` si queda sin uso tras estos cambios.
    7. En `PlazoServiceTest.java` (este test rompe al compilar en cuanto se borran los setters del paso 3 — hay que actualizarlo en el mismo task):
       a) Cambiar la firma del helper `buildPrecioOro` (líneas ~102-110) a `private PrecioOro buildPrecioOro(Integer baseKilataje)` — eliminar los parámetros `factorFundir`/`factorNormal`/`factorEspecial` y las 3 llamadas `precio.setFactorFundir/Normal/Especial(...)` de su cuerpo. El helper queda solo con `precio.setBaseKilataje(baseKilataje)`.
       b) Actualizar los call sites que quedan para pasar solo el `baseKilataje`: `buildPrecioOro(21, null, null, null)` -> `buildPrecioOro(21)` en `actualizarTodosPrecios_21K_Normal_coincideConCOCAE` (línea ~125) y en `actualizarTodosPrecios_celdaSinPorcPrestamo_lanzaResourceNotFoundException` (línea ~180).
       c) Eliminar por completo el test `actualizarTodosPrecios_ignoraFactoresHechuraGlobales` (líneas ~143-170, incluyendo su `@Test` y su comentario). Ese test existía únicamente para probar que los factores se ignoraban en el cálculo; una vez eliminados los campos ese comportamiento ya no existe y el test quedaría como duplicado exacto del test 21K/Normal, así que borrarlo lee más limpio que reescribirlo. No agregar un test nuevo — la ausencia de los campos ya la garantizan los `grep` de acceptance sobre entidad/DTOs.
  </action>
  <verify>
    <automated>cd prestamil-backend && ./mvnw -q -o compile 2>&1 | tail -20</automated>
    <automated>cd prestamil-backend && ./mvnw -q -o test-compile 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DROP COLUMN factor_fundir" prestamil-backend/src/main/resources/db/changelog/changes/013-drop-factores-hechura-precio-oro.sql` devuelve 1
    - `grep -c "013-drop-factores-hechura-precio-oro.sql" prestamil-backend/src/main/resources/db/changelog/db.changelog-master.xml` devuelve 1
    - `grep -rc "factorFundir\|factorNormal\|factorEspecial" prestamil-backend/src/main/java/com/ignis/prestamil/model/PrecioOro.java prestamil-backend/src/main/java/com/ignis/prestamil/request/PrecioOroRequest.java prestamil-backend/src/main/java/com/ignis/prestamil/response/PrecioOroResponse.java` devuelve 0 en los tres archivos
    - `grep -c "setFactorFundir\|setFactorNormal\|setFactorEspecial" prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java` devuelve 0
    - `grep -c "factorFundir\|factorNormal\|factorEspecial\|setFactorFundir\|setFactorNormal\|setFactorEspecial\|actualizarTodosPrecios_ignoraFactoresHechuraGlobales" prestamil-backend/src/test/java/com/ignis/prestamil/service/PlazoServiceTest.java` devuelve 0
    - El proyecto backend compila (`mvnw -o compile` termina en BUILD SUCCESS)
    - Las fuentes de test compilan (`mvnw -o test-compile` termina en BUILD SUCCESS; la salida no contiene "ERROR" ni "BUILD FAILURE")
  </acceptance_criteria>
  <done>Las columnas factor y sus referencias en entidad, DTOs, servicio y test están eliminadas; el backend compila y las fuentes de test compilan (test-compile en BUILD SUCCESS).</done>
</task>

<task type="auto">
  <name>Task 2: Método público de cascada en PlazoService (ORO-06)</name>
  <files>prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java</files>
  <read_first>
    - prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java (recalcularRegistros líneas ~325-354; recalcularTodasLasTablas como referencia de cómo carga registros y precio)
    - prestamil-backend/src/main/java/com/ignis/prestamil/repository/PlazoHechuraAlhajaRepository.java (confirmar firma findByIdSucursalId)
  </read_first>
  <action>
    Agregar a `PlazoService.java` un método público nuevo que dispare la cascada de recálculo reutilizando el `recalcularRegistros` privado ya existente (mismo mecanismo que D-14 exige). Firma y cuerpo exactos:
```java
/**
 * Recalcula precioBase y precioPrestamo de TODOS los PlazoHechuraAlhaja de la sucursal
 * cuando cambia el %Prestamo de una celda en oro_tabla_prestamo. Reutiliza la formula
 * COCAE de recalcularRegistros; preserva el porcAumento propio de cada plazo (ORO-06).
 *
 * @param sucursalId identificador de la sucursal cuya tabla de oro cambió
 * @throws ResourceNotFoundException si no hay precio de oro configurado para la sucursal
 */
public void recalcularPrecioBasePorTablaOro(Integer sucursalId) {
    com.ignis.prestamil.model.PrecioOro precio = precioOroRepository.findBySucursalId(sucursalId)
            .orElseThrow(() -> new ResourceNotFoundException(
                    "No hay precio de oro configurado para la sucursal " + sucursalId));
    List<PlazoHechuraAlhaja> registros = plazoHechuraAlhajaRepository.findByIdSucursalId(sucursalId);
    if (!registros.isEmpty()) {
        recalcularRegistros(registros, precio.getPrecioGramo24k(), 24, sucursalId);
        plazoHechuraAlhajaRepository.saveAll(registros);
    }
}
```
    Nota: se usa base 24 hardcodeada igual que `recalcularTodasLasTablas` (línea 381 `int baseKilataje = 24;`). No modificar `recalcularRegistros`.
  </action>
  <verify>
    <automated>cd prestamil-backend && ./mvnw -q -o compile 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public void recalcularPrecioBasePorTablaOro" prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java` devuelve 1
    - `grep -c "recalcularRegistros(registros, precio.getPrecioGramo24k(), 24, sucursalId)" prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java` devuelve 1
    - El proyecto backend compila (BUILD SUCCESS)
  </acceptance_criteria>
  <done>Existe un método público que recalcula en cascada reutilizando recalcularRegistros; compila.</done>
</task>

<task type="auto">
  <name>Task 3: DTOs + Service + Controller de OroTablaPrestamo (ORO-05, ORO-07)</name>
  <files>prestamil-backend/src/main/java/com/ignis/prestamil/response/OroCeldaResponse.java, prestamil-backend/src/main/java/com/ignis/prestamil/request/OroCeldaUpdateRequest.java, prestamil-backend/src/main/java/com/ignis/prestamil/service/OroTablaPrestamoService.java, prestamil-backend/src/main/java/com/ignis/prestamil/controller/OroTablaPrestamoController.java</files>
  <read_first>
    - prestamil-backend/src/main/java/com/ignis/prestamil/model/OroTablaPrestamo.java
    - prestamil-backend/src/main/java/com/ignis/prestamil/model/OroTablaPrestamoId.java
    - prestamil-backend/src/main/java/com/ignis/prestamil/repository/OroTablaPrestamoRepository.java
    - prestamil-backend/src/main/java/com/ignis/prestamil/controller/PrecioOroController.java (patrón de controller con @RequestParam sucursalId)
    - prestamil-backend/src/main/java/com/ignis/prestamil/exception/BadRequestException.java
    - prestamil-backend/src/main/java/com/ignis/prestamil/exception/ResourceNotFoundException.java
    - .planning/phases/04.1-configuracion-del-oro-admin-ui-para-tabla-de-24-celdas/04.1-CONTEXT.md (sección `<specifics>` con las fórmulas)
  </read_first>
  <action>
    Crear 4 archivos nuevos. Fórmulas (de CONTEXT `<specifics>`): `precioAvaluo = precioGramo24k / baseKilataje * kilataje`; `precioPrestamo(ref) = precioAvaluo * porcPrestamo / 100`. Redondeo: escala 4 HALF_UP en los valores devueltos.

    1. `response/OroCeldaResponse.java` — DTO de salida por celda (Lombok `@Getter @Setter`):
       campos: `Integer kilataje`, `String hechura`, `String hechuraDescripcion` ("Fundir"|"Normal"|"Especial"), `BigDecimal precioAvaluo`, `BigDecimal porcPrestamo`, `BigDecimal precioPrestamo`, `boolean editable`.

    2. `request/OroCeldaUpdateRequest.java` — DTO de entrada (Lombok `@Getter @Setter`):
       campo: `@NotNull @PositiveOrZero BigDecimal porcPrestamo;` (imports jakarta.validation.constraints).

    3. `service/OroTablaPrestamoService.java` — `@Service @Transactional @RequiredArgsConstructor`, inyecta `OroTablaPrestamoRepository`, `PrecioOroRepository`, `PlazoService`. Métodos:
       - `public List<OroCeldaResponse> getTabla(Integer sucursalId)`:
         a) Carga `precioOroRepository.findBySucursalId(sucursalId)`; si está vacío usa precioGramo24k = BigDecimal.ZERO y baseKilataje = 24 (para no romper cuando aún no hay precio).
         b) Carga las celdas con `oroTablaPrestamoRepository.findByIdSucursalId(sucursalId)`.
         c) Para cada celda: `precioAvaluo = precioGramo24k.divide(base,10,HALF_UP).multiply(kilataje).setScale(4,HALF_UP)`; `precioPrestamo = precioAvaluo.multiply(porcPrestamo.divide(100,10,HALF_UP)).setScale(4,HALF_UP)`. `editable = kilataje != 24`. `hechuraDescripcion` desde el código: F->"Fundir", N->"Normal", E->"Especial".
         d) Devuelve la lista ordenada por kilataje asc y luego hechura (F,N,E).
       - `public OroCeldaResponse actualizarCelda(Integer sucursalId, Integer kilataje, String hechura, OroCeldaUpdateRequest request)`:
         a) Si `kilataje == 24` lanzar `new BadRequestException("El oro de 24K no es prendable; su %Prestamo no es editable")` (ORO-07/D-12).
         b) Buscar la celda por `new OroTablaPrestamoId(sucursalId, kilataje, hechura)` vía `oroTablaPrestamoRepository.findById(...)`; si no existe lanzar `ResourceNotFoundException`.
         c) `entity.setPorcPrestamo(request.getPorcPrestamo()); entity.setActualizadoEn(LocalDateTime.now()); oroTablaPrestamoRepository.save(entity);`
         d) Disparar cascada: `plazoService.recalcularPrecioBasePorTablaOro(sucursalId);`
         e) Devolver la celda recalculada reutilizando el mapeo de `getTabla` (o construyendo el `OroCeldaResponse` con las mismas fórmulas).

    4. `controller/OroTablaPrestamoController.java` — `@RestController @RequestMapping("/api/oro-tabla-prestamo") @RequiredArgsConstructor`, inyecta `OroTablaPrestamoService`. Endpoints:
       - `@GetMapping public ResponseEntity<List<OroCeldaResponse>> getTabla(@RequestParam(defaultValue = "1") Integer sucursalId)` -> `ok(service.getTabla(sucursalId))`.
       - `@PutMapping("/{kilataje}/{hechura}") public ResponseEntity<OroCeldaResponse> actualizarCelda(@PathVariable Integer kilataje, @PathVariable String hechura, @RequestParam(defaultValue = "1") Integer sucursalId, @Valid @RequestBody OroCeldaUpdateRequest request)` -> `ok(service.actualizarCelda(sucursalId, kilataje, hechura, request))`.

    Usar convenciones del proyecto: `@Column`/DTOs sin exponer entidad, Javadoc en español en métodos públicos del servicio, `BadRequestException` para reglas de negocio.
  </action>
  <verify>
    <automated>cd prestamil-backend && ./mvnw -q -o compile 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Existen los 4 archivos: `response/OroCeldaResponse.java`, `request/OroCeldaUpdateRequest.java`, `service/OroTablaPrestamoService.java`, `controller/OroTablaPrestamoController.java`
    - `grep -c "@RequestMapping(\"/api/oro-tabla-prestamo\")" prestamil-backend/src/main/java/com/ignis/prestamil/controller/OroTablaPrestamoController.java` devuelve 1
    - `grep -c "kilataje == 24\|kilataje.equals(24)\|24 == kilataje" prestamil-backend/src/main/java/com/ignis/prestamil/service/OroTablaPrestamoService.java` es >= 1 (rechazo 24K)
    - `grep -c "recalcularPrecioBasePorTablaOro" prestamil-backend/src/main/java/com/ignis/prestamil/service/OroTablaPrestamoService.java` devuelve 1
    - `grep -c "@GetMapping\|@PutMapping" prestamil-backend/src/main/java/com/ignis/prestamil/controller/OroTablaPrestamoController.java` es >= 2
    - El proyecto backend compila (BUILD SUCCESS)
  </acceptance_criteria>
  <done>Los endpoints GET (24 celdas computadas) y PUT (edita %Prestamo con rechazo 24K y cascada) existen y compilan.</done>
</task>

</tasks>

<verification>
- `./mvnw -o compile` termina en BUILD SUCCESS.
- `./mvnw -o test-compile` termina en BUILD SUCCESS (las fuentes de test siguen compilando tras eliminar los factores).
- Changeset 013 registrado en el master y con rollback explícito.
- El endpoint GET devuelve 24 celdas; el PUT sobre 24K responde 400; el PUT sobre kilataje prendable dispara la cascada.
</verification>

<success_criteria>
- ORO-08 backend: precio_oro y sus DTOs/entidad ya no tienen factores de hechura, y las fuentes de test compilan sin ellos.
- ORO-05/ORO-07 backend: existen endpoints para listar y editar %Prestamo con la regla 24K no editable.
- ORO-06: editar una celda recalcula precioBase en todos los PlazoHechuraAlhaja preservando porcAumento.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-configuracion-del-oro-admin-ui-para-tabla-de-24-celdas/04.1-01-SUMMARY.md`
</output>
</content>
</invoke>
