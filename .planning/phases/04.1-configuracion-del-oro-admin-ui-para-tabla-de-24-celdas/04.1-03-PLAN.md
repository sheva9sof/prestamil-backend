---
phase: 04.1-configuracion-del-oro-admin-ui-para-tabla-de-24-celdas
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.html
  - prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.ts
  - prestamil-frontend/src/app/prestamil/core/models/plazo.model.ts
autonomous: true
requirements: [ORO-08]

must_haves:
  truths:
    - "La tarjeta 'Precio del Oro (global)' ya no aparece en /plazos-periodos"
    - "El componente plazos-periodos ya no referencia precioOro ni los factores de hechura"
    - "Los tipos PrecioOroRequest/PrecioOroResponse en plazo.model.ts ya no tienen campos factor"
    - "El frontend compila sin errores tras eliminar las referencias"
  artifacts:
    - path: "prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.html"
      provides: "Vista de plazos sin la tarjeta de precio del oro global"
    - path: "prestamil-frontend/src/app/prestamil/core/models/plazo.model.ts"
      provides: "Tipos PrecioOro sin factores"
  key_links:
    - from: "plazos-periodos.component.ts"
      to: "plazo.model.ts"
      via: "ya no usa factorFundir/Normal/Especial ni recalcularGlobal"
      pattern: "factorFundir"
---

<objective>
Retirar de la pantalla `/plazos-periodos` la tarjeta "Precio del Oro (global)" (D-16, ese control se mudó a la nueva pantalla Configuración del Oro) y eliminar del frontend las referencias a los 3 factores de hechura (ORO-08), alineando `plazo.model.ts` con el backend ya limpio en Plan 01.

Purpose: Evitar dos fuentes de precio que confunden al usuario y terminar la limpieza de código muerto de los factores en el frontend.
Output: HTML sin la tarjeta, componente TS sin el estado/métodos del precio global y factores, modelo sin campos factor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-configuracion-del-oro-admin-ui-para-tabla-de-24-celdas/04.1-CONTEXT.md
@.planning/phases/04.1-configuracion-del-oro-admin-ui-para-tabla-de-24-celdas/04.1-01-SUMMARY.md

<!-- La tarjeta a eliminar vive en plazos-periodos.component.html líneas ~152-243 (bloque
     <!-- Precio del Oro: control GLOBAL ... --> ... </app-card>). El estado/métodos asociados
     en el TS: propiedades precioOro, precioOroInput, factorFundirInput/NormalInput/EspecialInput,
     precioOroBaseKilataje, precioOroCalcularSobre, precioOroError; métodos cargarPrecioOro()
     y recalcularGlobal(); la llamada a cargarPrecioOro() en ngOnInit. -->
<!-- IMPORTANTE: NO eliminar el recálculo por-plazo existente (precioBaseOroInput, recalcularTodo(),
     recalcularTodosPrecios en el servicio) — ese es un control distinto que se conserva. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminar la tarjeta "Precio del Oro (global)" del HTML</name>
  <files>prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.html</files>
  <read_first>
    - prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.html (bloque líneas ~152-243)
  </read_first>
  <action>
    Eliminar completo el bloque de la tarjeta de precio global: desde el comentario `<!-- Precio del Oro: control GLOBAL, fuera de los registros de cada plazo.` (~línea 152) hasta el `</app-card>` de cierre de esa tarjeta (~línea 243), inclusive. Conservar intacta la tarjeta siguiente `<!-- Tarjeta principal: lista completa de plazos -->` (~línea 245 en adelante). No tocar el bloque de recálculo por-plazo dentro del modal de detalle (precioBaseOroInput / recalcularTodo).
  </action>
  <verify>
    <automated>cd prestamil-frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Precio del Oro (global)" prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.html` devuelve 0
    - `grep -c "recalcularGlobal()" prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.html` devuelve 0
    - `grep -c "factorFundirInput\|factorNormalInput\|factorEspecialInput" prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.html` devuelve 0
    - `grep -c "Tarjeta principal: lista completa de plazos" prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.html` devuelve 1 (se conserva)
  </acceptance_criteria>
  <done>La tarjeta de precio global desapareció del HTML; la lista de plazos y el recálculo por-plazo siguen presentes.</done>
</task>

<task type="auto">
  <name>Task 2: Limpiar estado/métodos del precio global y factores en el componente TS</name>
  <files>prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.ts</files>
  <read_first>
    - prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.ts (propiedades líneas ~77-86; ngOnInit ~106-110; cargarPrecioOro ~116-127; recalcularGlobal ~133-163)
  </read_first>
  <action>
    En `plazos-periodos.component.ts`:
    1. Eliminar las propiedades del precio global y factores (bloque ~líneas 77-86): `precioOro`, `precioOroInput`, `precioOroBaseKilataje`, `precioOroCalcularSobre`, `factorFundirInput`, `factorNormalInput`, `factorEspecialInput`, `precioOroError`.
    2. Eliminar los métodos `cargarPrecioOro()` (~116-127) y `recalcularGlobal()` (~133-163) completos.
    3. En `ngOnInit()`, eliminar la llamada `this.cargarPrecioOro();` (conservar `this.loadPlazos();` y `this.cargarTiposPrenda();`).
    4. En los imports de tipos desde `'../../../core/models/plazo.model'`, quitar `PrecioOroResponse` y `PrecioOroRequest` de la lista de imports si quedan sin uso tras los cambios (verificar que ninguna otra parte del archivo los use; `getPrecioOro`/`actualizarPrecioOroGlobal` del servicio dejan de invocarse desde aquí).
    NO eliminar: `precioBaseOroInput`, `isRecalculando`, `recalcularTodo()` (recálculo por-plazo, se conserva).
  </action>
  <verify>
    <automated>cd prestamil-frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cargarPrecioOro\|recalcularGlobal" prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.ts` devuelve 0
    - `grep -c "factorFundirInput\|factorNormalInput\|factorEspecialInput\|precioOroInput\|precioOroError" prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.ts` devuelve 0
    - `grep -c "recalcularTodo\|precioBaseOroInput" prestamil-frontend/src/app/prestamil/pages/configuracion/plazos-periodos/plazos-periodos.component.ts` es >= 1 (conservado)
    - `npx tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>El componente ya no tiene estado ni métodos del precio global/factores; el recálculo por-plazo permanece; compila.</done>
</task>

<task type="auto">
  <name>Task 3: Quitar campos factor de los tipos PrecioOro en plazo.model.ts</name>
  <files>prestamil-frontend/src/app/prestamil/core/models/plazo.model.ts</files>
  <read_first>
    - prestamil-frontend/src/app/prestamil/core/models/plazo.model.ts (PrecioOroRequest líneas ~100-108; PrecioOroResponse ~110-121)
    - .planning/phases/04.1-configuracion-del-oro-admin-ui-para-tabla-de-24-celdas/04.1-01-SUMMARY.md (confirmar que el backend ya no devuelve factores)
  </read_first>
  <action>
    En `plazo.model.ts`:
    1. En `PrecioOroRequest`: eliminar `factorFundir?`, `factorNormal?`, `factorEspecial?` y su comentario (líneas ~104-107). Conservar `precioGramoBase`, `baseKilataje?`, `calcularSobre?`.
    2. En `PrecioOroResponse`: eliminar `factorFundir: number;`, `factorNormal: number;`, `factorEspecial: number;` (líneas ~116-118). Conservar el resto.
    Nota: estos tipos pueden quedar aún referenciados por el servicio `plazo.service.ts` (métodos `getPrecioOro`/`actualizarPrecioOroGlobal`); se conservan esos métodos del servicio (no se usan desde plazos-periodos tras Plan 03, pero eliminarlos no es requerido). Solo asegurarse de que la firma siga compilando sin los campos factor.
  </action>
  <verify>
    <automated>cd prestamil-frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "factorFundir\|factorNormal\|factorEspecial" prestamil-frontend/src/app/prestamil/core/models/plazo.model.ts` devuelve 0
    - `grep -c "precioGramoBase" prestamil-frontend/src/app/prestamil/core/models/plazo.model.ts` es >= 1 (conservado)
    - `npx tsc --noEmit` sin errores en todo el frontend
  </acceptance_criteria>
  <done>Los tipos PrecioOro del frontend ya no tienen campos factor y el frontend compila.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` sin errores.
- /plazos-periodos ya no muestra la tarjeta de precio del oro global; la lista de plazos y el recálculo por-plazo funcionan igual.
</verification>

<success_criteria>
- ORO-08 (frontend): la UI y los tipos del frontend ya no contienen los factores de hechura, y la tarjeta de precio global se retiró de /plazos-periodos (D-16).
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-configuracion-del-oro-admin-ui-para-tabla-de-24-celdas/04.1-03-SUMMARY.md`
</output>
