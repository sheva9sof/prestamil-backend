---
phase: 04-motor-de-oro-fidelidad-cocae-cierre-de-brecha-de-confianza
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - prestamil-backend/src/main/java/com/ignis/prestamil/service/ContratoService.java
  - prestamil-backend/src/test/java/com/ignis/prestamil/service/ContratoServiceTest.java
autonomous: true
requirements: [ORO-03, ORO-04]

must_haves:
  truths:
    - "Al crear un contrato con una partida ALHAJA, el avaluoReal que queda registrado es el que calcula el servidor a partir de PlazoHechuraAlhaja (kilataje, hechura, peso) — un avaluoReal distinto enviado por el cliente no tiene efecto (D-07)"
    - "Una partida ALHAJA con kilataje 24K es rechazada con BadRequestException y mensaje 'Oro de 24K no es prendable' (D-04), no con el mensaje generico de prestamo minimo"
    - "Un kilataje que no es ninguno de los 8 soportados por COCAE (6/8/10/12/14/18/21/24) es rechazado con BadRequestException 'Kilataje no soportado: {valor}' (D-05)"
    - "El monto de prestamo maximo de una partida ALHAJA se calcula a partir del avaluoReal recalculado por el servidor, nunca del avaluoReal enviado por el cliente"
  artifacts:
    - path: "prestamil-backend/src/main/java/com/ignis/prestamil/service/ContratoService.java"
      provides: "calcularAvaluoRealAlhaja server-side + buildPartida/calcularPrestamoMaximo ya no confian en pr.getAvaluoReal() para ALHAJA"
      contains: "calcularAvaluoRealAlhaja"
    - path: "prestamil-backend/src/test/java/com/ignis/prestamil/service/ContratoServiceTest.java"
      provides: "pruebas de regresion de brecha de confianza (avaluoReal spoofed ignorado) y de rechazos D-04/D-05"
      contains: "ignoraAvaluoRealDelCliente"
  key_links:
    - from: "prestamil-backend/src/main/java/com/ignis/prestamil/service/ContratoService.java"
      to: "prestamil-backend/src/main/java/com/ignis/prestamil/repository/PlazoHechuraAlhajaRepository.java"
      via: "plazoHechuraAlhajaRepository.findById(new PlazoHechuraAlhajaId(...)) dentro de calcularAvaluoRealAlhaja"
      pattern: "plazoHechuraAlhajaRepository\\.findById"
---

<objective>
Cerrar la brecha de confianza servidor/cliente para partidas ALHAJA: hoy `ContratoService.buildPartida()` persiste directamente `pr.getAvaluoReal()` (valor que envia el cliente) y `calcularPrestamoMaximo()` lo usa como techo del prestamo — un cliente comprometido o un bug de frontend puede inflar el avaluo para obtener un prestamo mayor al que las reglas de negocio permiten (Pitfall 1, milestone PITFALLS.md).

Purpose: Satisface ORO-03 (el servidor recalcula el avaluo real de ALHAJA a partir de `PlazoHechuraAlhaja`) y la porcion de ORO-04 correspondiente al lado `ContratoService` (verificacion con pruebas que el avaluo calculado coincide con la tabla de precios del plazo y que un valor spoofed del cliente no tiene efecto).
Output: `ContratoService.calcularAvaluoRealAlhaja()` nuevo (server-side, con validaciones D-04/D-05), `buildPartida()`/`calcularPrestamoMaximo()` ya no leen `pr.getAvaluoReal()` para partidas ALHAJA; `ContratoServiceTest.java` nuevo con pruebas de regresion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-motor-de-oro-fidelidad-cocae-cierre-de-brecha-de-confianza/04-CONTEXT.md
@.planning/phases/04-motor-de-oro-fidelidad-cocae-cierre-de-brecha-de-confianza/04-RESEARCH.md
@prestamil-backend/src/main/java/com/ignis/prestamil/service/ContratoService.java
@prestamil-backend/src/test/java/com/ignis/prestamil/service/UsuarioServiceTest.java

<interfaces>
<!-- Contratos/tipos que el executor necesita, extraidos directamente del codebase -->

From prestamil-backend/src/main/java/com/ignis/prestamil/model/PlazoHechuraAlhaja.java (ya existe, sin cambios):
```java
@Entity @Table(name = "plazo_hechura_alhaja")
public class PlazoHechuraAlhaja {
    @EmbeddedId private PlazoHechuraAlhajaId id;
    private BigDecimal precioBase;      // precision 12, scale 4
    private BigDecimal porcAumento;     // precision 7, scale 4
    private BigDecimal precioPrestamo;  // precision 12, scale 4 — este es el que usa calcularAvaluoRealAlhaja
}
```

From prestamil-backend/src/main/java/com/ignis/prestamil/model/PlazoHechuraAlhajaId.java (ya existe, sin cambios):
```java
public class PlazoHechuraAlhajaId implements Serializable {
    private Integer idPlazo;
    private Integer sucursalId;
    private Integer kilataje;
    private String hechura; // "F"/"N"/"E"
    // constructor AllArgsConstructor(idPlazo, sucursalId, kilataje, hechura)
}
```

From prestamil-backend/src/main/java/com/ignis/prestamil/repository/PlazoHechuraAlhajaRepository.java (ya existe, sin cambios):
```java
public interface PlazoHechuraAlhajaRepository extends BaseRepository<PlazoHechuraAlhaja, PlazoHechuraAlhajaId> {
    List<PlazoHechuraAlhaja> findByIdIdPlazoAndIdSucursalId(Integer idPlazo, Integer sucursalId);
    List<PlazoHechuraAlhaja> findByIdSucursalId(Integer sucursalId);
    // findById(PlazoHechuraAlhajaId) heredado de JpaRepository/BaseRepository
}
```

From prestamil-backend/src/main/java/com/ignis/prestamil/model/TipoPrenda.java (id=1 es "ALHAJA", confirmado en 002-initial-data.sql):
```java
@Entity @Table(name = "tipo_prenda")
public class TipoPrenda {
    private Integer id;
    private String tipo; // p.ej. "ALHAJA"
}
```

From prestamil-backend/src/main/java/com/ignis/prestamil/request/PartidaContratoRequest.java (ya existe, SIN cambios en este plan):
```java
public class PartidaContratoRequest {
    @NotNull private Integer idTipoPrenda;
    private BigDecimal pesoGramos;
    private Integer kilataje;
    private String hechura;
    @NotNull private BigDecimal avaluoReal;   // para ALHAJA este valor se IGNORA tras este plan
    private BigDecimal montoPrestamo;
    // ... otros campos sin relacion con este plan
}
```

<!-- Estado ACTUAL de ContratoService.buildPartida/calcularPrestamoMaximo — leer el archivo completo antes de editar -->
```java
private PartidaContrato buildPartida(PartidaContratoRequest pr, int numPartida,
                                     Long plazoId, Integer sucursalId) {
    TipoPrenda tipoPrenda = tipoPrendaRepository.findById(pr.getIdTipoPrenda())
            .orElseThrow(() -> new ResourceNotFoundException("TipoPrenda no encontrado: " + pr.getIdTipoPrenda()));
    PlazoParametro parametro = plazoParametroRepository
            .findByPlazoIdAndTipoPrendaIdAndSucursalId(plazoId, tipoPrenda.getId(), sucursalId)
            .orElse(null);
    BigDecimal prestamoMaximo = calcularPrestamoMaximo(pr, parametro);   // <-- lee pr.getAvaluoReal() internamente, HOY
    // ... validaciones de montoPrestamo contra prestamoMaximo ...
    BigDecimal avaluoContrato = parametro != null
            ? plazoService.calcularAvaluoContrato(pr.getMontoPrestamo(), parametro)
            : pr.getMontoPrestamo();
    PartidaContrato partida = new PartidaContrato();
    // ... setters ...
    partida.setAvaluoReal(pr.getAvaluoReal());   // <-- BRECHA DE CONFIANZA, se reemplaza en este plan
    partida.setAvaluoContrato(avaluoContrato);
    // ...
    return partida;
}

private BigDecimal calcularPrestamoMaximo(PartidaContratoRequest pr, PlazoParametro parametro) {
    BigDecimal avaluo = pr.getAvaluoReal() != null ? pr.getAvaluoReal() : BigDecimal.ZERO;   // <-- lee del cliente
    // ...
}
```

Constructor actual de ContratoService (agregar `PlazoHechuraAlhajaRepository` como ULTIMO parametro, sin reordenar los existentes):
```java
public ContratoService(ContratoRepository repository,
                       ClienteRepository clienteRepository,
                       PlazoRepository plazoRepository,
                       TurnoRepository turnoRepository,
                       UsuarioRepository usuarioRepository,
                       TipoPrendaRepository tipoPrendaRepository,
                       CatValorPrendaRepository catValorPrendaRepository,
                       ContratoMapper contratoMapper,
                       PlazoParametroRepository plazoParametroRepository,
                       PlazoService plazoService) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: calcularAvaluoRealAlhaja server-side + cierre de brecha en buildPartida/calcularPrestamoMaximo</name>
  <files>prestamil-backend/src/main/java/com/ignis/prestamil/service/ContratoService.java</files>
  <read_first>
    prestamil-backend/src/main/java/com/ignis/prestamil/service/ContratoService.java
    prestamil-backend/src/main/java/com/ignis/prestamil/model/PlazoHechuraAlhaja.java
    prestamil-backend/src/main/java/com/ignis/prestamil/model/PlazoHechuraAlhajaId.java
    prestamil-backend/src/main/java/com/ignis/prestamil/model/TipoPrenda.java
    prestamil-backend/src/main/java/com/ignis/prestamil/request/PartidaContratoRequest.java
  </read_first>
  <behavior>
    - Dado un plazoId=1, sucursalId=1, kilataje=14, hechura="N", pesoGramos=10.0000, y una fila PlazoHechuraAlhaja(1,1,14,"N") con precioPrestamo=1200.0000 en el repositorio: calcularAvaluoRealAlhaja debe devolver 12000.00 (precioPrestamo x pesoGramos, escala 2 HALF_UP), sin importar que pr.getAvaluoReal() venga con un valor distinto
    - Dado kilataje=24: se lanza BadRequestException con mensaje exactamente "Oro de 24K no es prendable", ANTES de consultar el repositorio de PlazoHechuraAlhaja
    - Dado kilataje=16 (no esta en {6,8,10,12,14,18,21,24}): se lanza BadRequestException con mensaje exactamente "Kilataje no soportado: 16"
    - Dado pesoGramos=null o <=0: se lanza BadRequestException (peso requerido)
    - Dado hechura=null o en blanco: se lanza BadRequestException (hechura requerida), no un ResourceNotFoundException confuso
    - Al crear un contrato con una partida ALHAJA valida, la partida persistida (PartidaContrato.avaluoReal) es el valor CALCULADO por el servidor, no pr.getAvaluoReal() — incluso si pr.getAvaluoReal() es artificialmente alto (999999.00)
    - Para partidas NO-ALHAJA (p.ej. Varios/electronicos), el comportamiento no cambia: se sigue usando pr.getAvaluoReal() tal cual (avaluo libre del valuador, fuera de alcance de este phase)
  </behavior>
  <action>
En `prestamil-backend/src/main/java/com/ignis/prestamil/service/ContratoService.java`:

1. Agregar import: `com.ignis.prestamil.model.PlazoHechuraAlhaja`, `com.ignis.prestamil.model.PlazoHechuraAlhajaId`, `com.ignis.prestamil.repository.PlazoHechuraAlhajaRepository`, `java.util.List` (ya deberia estar importado; verificar). Nota: `import com.ignis.prestamil.model.*;` y `import com.ignis.prestamil.repository.*;` ya existen en el archivo — confirmar que cubren estas clases antes de agregar imports duplicados.

2. Agregar el campo `private final PlazoHechuraAlhajaRepository plazoHechuraAlhajaRepository;` y agregarlo como ULTIMO parametro del constructor (no reordenar los parametros existentes), asignandolo igual que los demas campos `final`.

3. Agregar la constante de clase (junto a otros campos estaticos, o al inicio de la clase si no existen otros):
```java
private static final List<Integer> KILATAJES_COCAE = List.of(6, 8, 10, 12, 14, 18, 21, 24);
```

4. Agregar el metodo privado nuevo `calcularAvaluoRealAlhaja` (ubicarlo junto a `calcularPrestamoMaximo` en la seccion "Helpers privados"):
```java
    /**
     * Recalcula el avalúo real de una partida ALHAJA a partir de la tabla de precios
     * del plazo (PlazoHechuraAlhaja), ignorando el avaluoReal que envía el cliente.
     * Cierra la brecha de confianza servidor/cliente (D-07, Pitfall 1 de PITFALLS.md).
     *
     * @param pr         datos de la partida solicitada
     * @param plazoId    identificador del plazo (se convierte a Integer explícitamente —
     *                   PlazoHechuraAlhajaId usa Integer, Plazo.id es Long)
     * @param sucursalId identificador de la sucursal
     * @return avalúo real calculado por el servidor, escala 2 (HALF_UP)
     * @throws BadRequestException si el kilataje es 24K, no soportado, o falta peso/hechura
     * @throws ResourceNotFoundException si no existe tabla de precios para la celda
     */
    private BigDecimal calcularAvaluoRealAlhaja(PartidaContratoRequest pr, Long plazoId, Integer sucursalId) {
        Integer kilataje = pr.getKilataje();
        if (kilataje == null) {
            throw new BadRequestException("Kilataje es requerido para partidas de tipo ALHAJA");
        }
        if (kilataje == 24) {
            throw new BadRequestException("Oro de 24K no es prendable");
        }
        if (!KILATAJES_COCAE.contains(kilataje)) {
            throw new BadRequestException("Kilataje no soportado: " + kilataje);
        }
        if (pr.getHechura() == null || pr.getHechura().isBlank()) {
            throw new BadRequestException("Hechura es requerida para partidas de tipo ALHAJA");
        }
        if (pr.getPesoGramos() == null || pr.getPesoGramos().compareTo(BigDecimal.ZERO) <= 0) {
            throw new BadRequestException("Peso en gramos debe ser mayor que cero para partidas ALHAJA");
        }
        String hechura = pr.getHechura();
        PlazoHechuraAlhajaId id = new PlazoHechuraAlhajaId(Math.toIntExact(plazoId), sucursalId, kilataje, hechura);
        PlazoHechuraAlhaja tabla = plazoHechuraAlhajaRepository.findById(id)
                .orElseThrow(() -> new ResourceNotFoundException(
                        "No hay precio configurado para plazo=" + plazoId + ", kilataje=" + kilataje
                        + ", hechura=" + hechura));
        return tabla.getPrecioPrestamo()
                .multiply(pr.getPesoGramos())
                .setScale(2, RoundingMode.HALF_UP);
    }

    /**
     * Determina si un tipo de prenda corresponde a ALHAJA (oro), comparando por texto
     * en vez del id hardcodeado para mayor robustez.
     *
     * @param tipoPrenda tipo de prenda a evaluar
     * @return true si tipoPrenda.getTipo() es "ALHAJA" (case-insensitive)
     */
    private boolean esAlhaja(TipoPrenda tipoPrenda) {
        return tipoPrenda != null && "ALHAJA".equalsIgnoreCase(tipoPrenda.getTipo());
    }
```

5. Reemplazar el cuerpo de `buildPartida` para calcular `avaluoReal` ANTES de `calcularPrestamoMaximo` y usarlo en vez de `pr.getAvaluoReal()`:
```java
    private PartidaContrato buildPartida(PartidaContratoRequest pr, int numPartida,
                                         Long plazoId, Integer sucursalId) {
        TipoPrenda tipoPrenda = tipoPrendaRepository.findById(pr.getIdTipoPrenda())
                .orElseThrow(() -> new ResourceNotFoundException(
                        "TipoPrenda no encontrado: " + pr.getIdTipoPrenda()));

        // Parámetros del plazo/tipo de prenda/sucursal (puede no existir → reglas por defecto)
        PlazoParametro parametro = plazoParametroRepository
                .findByPlazoIdAndTipoPrendaIdAndSucursalId(plazoId, tipoPrenda.getId(), sucursalId)
                .orElse(null);

        // Avalúo real: para ALHAJA lo calcula el servidor a partir de PlazoHechuraAlhaja (D-07).
        // Para otros tipos (Varios/electrónicos, avalúo libre del valuador) se conserva el valor del cliente.
        BigDecimal avaluoReal = esAlhaja(tipoPrenda)
                ? calcularAvaluoRealAlhaja(pr, plazoId, sucursalId)
                : (pr.getAvaluoReal() != null ? pr.getAvaluoReal() : BigDecimal.ZERO);

        // Préstamo máximo autorizado para esta partida, a partir del avalúo YA recalculado por el servidor
        BigDecimal prestamoMaximo = calcularPrestamoMaximo(avaluoReal, parametro);

        // El préstamo solicitado NUNCA puede superar el máximo (solo ajuste hacia abajo)
        if (pr.getMontoPrestamo() == null || pr.getMontoPrestamo().compareTo(BigDecimal.ZERO) <= 0) {
            throw new BadRequestException("Partida " + numPartida + ": el monto de préstamo debe ser mayor que cero");
        }
        if (pr.getMontoPrestamo().compareTo(prestamoMaximo) > 0) {
            throw new BadRequestException(String.format(
                    "Partida %d: el préstamo (%s) supera el máximo autorizado (%s)",
                    numPartida, pr.getMontoPrestamo(), prestamoMaximo));
        }
        // Importe mínimo de préstamo configurado
        if (parametro != null && parametro.getImporteMinPrestamo() != null
                && parametro.getImporteMinPrestamo().compareTo(BigDecimal.ZERO) > 0
                && pr.getMontoPrestamo().compareTo(parametro.getImporteMinPrestamo()) < 0) {
            throw new BadRequestException(String.format(
                    "Partida %d: el préstamo (%s) es menor al mínimo permitido (%s)",
                    numPartida, pr.getMontoPrestamo(), parametro.getImporteMinPrestamo()));
        }

        // Avalúo del contrato: lo fija el servidor (no se confía en el valor del cliente)
        BigDecimal avaluoContrato = parametro != null
                ? plazoService.calcularAvaluoContrato(pr.getMontoPrestamo(), parametro)
                : pr.getMontoPrestamo();

        PartidaContrato partida = new PartidaContrato();
        partida.setNumPartida(numPartida);
        partida.setTipoPrenda(tipoPrenda);
        partida.setDescripcion(pr.getDescripcion());
        partida.setClavePrenda(pr.getClavePrenda());
        partida.setCantidad(pr.getCantidad() != null ? pr.getCantidad() : 1);
        partida.setPesoGramos(pr.getPesoGramos());
        partida.setKilataje(pr.getKilataje());
        partida.setLey(pr.getLey());
        partida.setHechura(pr.getHechura());
        partida.setPrecioXGramo(pr.getPrecioXGramo());
        partida.setAvaluoReal(avaluoReal);   // valor calculado por el servidor, NO pr.getAvaluoReal()
        partida.setAvaluoContrato(avaluoContrato);
        partida.setMontoPrestamo(pr.getMontoPrestamo());
        partida.setSubtipo(pr.getSubtipo());
        partida.setMarca(pr.getMarca());
        partida.setModelo(pr.getModelo());
        partida.setSerieImei(pr.getSerieImei());
        partida.setEstadoFisico(pr.getEstadoFisico());

        if (pr.getIdValorPrenda() != null) {
            CatValorPrenda valorPrenda = catValorPrendaRepository.findById(pr.getIdValorPrenda())
                    .orElseThrow(() -> new ResourceNotFoundException(
                            "ValorPrenda no encontrado: " + pr.getIdValorPrenda()));
            partida.setValorPrenda(valorPrenda);
        }

        return partida;
    }
```

6. Cambiar la firma y el cuerpo de `calcularPrestamoMaximo` para recibir el avalúo YA calculado en vez de leer `pr.getAvaluoReal()`:
```java
    /**
     * Calcula el préstamo máximo autorizado para una partida según el tipo de prenda:
     *   - El tope base es el avalúo YA recalculado por el servidor (avaluoReal).
     *   - Si el parámetro define un % de préstamo sobre avalúo (porcPrestamoSAvaluo > 0),
     *     el máximo es avaluoReal * porcPrestamoSAvaluo / 100.
     *   - Para prendas de libre avalúo (varios/electrónicos/autos) sin % configurado,
     *     el tope es el propio avalúo real (no se aplica la regla del oro).
     *
     * @param avaluoReal avalúo real ya calculado (server-side para ALHAJA, del cliente para el resto)
     * @param parametro  parámetros del plazo (puede ser null)
     * @return préstamo máximo autorizado, escala 2 (HALF_UP)
     */
    private BigDecimal calcularPrestamoMaximo(BigDecimal avaluoReal, PlazoParametro parametro) {
        BigDecimal avaluo = avaluoReal != null ? avaluoReal : BigDecimal.ZERO;
        if (parametro != null
                && parametro.getPorcPrestamoSAvaluo() != null
                && parametro.getPorcPrestamoSAvaluo().compareTo(BigDecimal.ZERO) > 0) {
            return avaluo
                    .multiply(parametro.getPorcPrestamoSAvaluo())
                    .divide(new BigDecimal("100"), 2, RoundingMode.HALF_UP);
        }
        return avaluo.setScale(2, RoundingMode.HALF_UP);
    }
```

7. No modificar `crearContrato`, `getByFolio`, `getById`, `getContratosPorCliente`, `getContratosVencidos`, `calcularAmortizacion` — sus firmas y comportamiento no cambian en este plan.
  </action>
  <verify>
    <automated>cd prestamil-backend && mvn -q compile</automated>
  </verify>
  <acceptance_criteria>
    - `mvn compile` en `prestamil-backend` termina con BUILD SUCCESS
    - `ContratoService.java` contiene la cadena `calcularAvaluoRealAlhaja`
    - `ContratoService.java` contiene la cadena `"Oro de 24K no es prendable"`
    - `ContratoService.java` contiene la cadena `"Kilataje no soportado: "`
    - La firma de `calcularPrestamoMaximo` en el archivo es `private BigDecimal calcularPrestamoMaximo(BigDecimal avaluoReal, PlazoParametro parametro)` (ya no recibe `PartidaContratoRequest`)
    - `buildPartida` ya NO contiene la cadena `partida.setAvaluoReal(pr.getAvaluoReal())`
  </acceptance_criteria>
  <done>ContratoService recalcula el avaluo real de partidas ALHAJA server-side a partir de PlazoHechuraAlhaja, rechaza 24K y kilatajes no soportados con mensajes claros, y calcularPrestamoMaximo ya no lee pr.getAvaluoReal() directamente.</done>
</task>

<task type="auto">
  <name>Task 2: ContratoServiceTest — regresion de brecha de confianza y rechazos D-04/D-05</name>
  <files>prestamil-backend/src/test/java/com/ignis/prestamil/service/ContratoServiceTest.java</files>
  <read_first>
    prestamil-backend/src/test/java/com/ignis/prestamil/service/UsuarioServiceTest.java
    prestamil-backend/src/main/java/com/ignis/prestamil/service/ContratoService.java
    prestamil-backend/src/main/java/com/ignis/prestamil/model/Contrato.java
    prestamil-backend/src/main/java/com/ignis/prestamil/model/PartidaContrato.java
    prestamil-backend/src/main/java/com/ignis/prestamil/model/PlazoParametro.java
    prestamil-backend/src/main/java/com/ignis/prestamil/request/ContratoRequest.java
    prestamil-backend/src/main/java/com/ignis/prestamil/request/PartidaContratoRequest.java
  </read_first>
  <action>
Crear `prestamil-backend/src/test/java/com/ignis/prestamil/service/ContratoServiceTest.java` (archivo nuevo — no existe ningun test hoy para esta clase; `ContratoTestMain.java` en `src/test/java/.../util/` es un script manual no relacionado, no tocarlo). Seguir EXACTAMENTE el estilo de `UsuarioServiceTest.java`: `@ExtendWith(MockitoExtension.class)`, `@Mock` para cada dependencia, instanciacion manual de `contratoService` en `@BeforeEach` (sin `@InjectMocks`), AssertJ (`assertThat`), `assertThat(actual.compareTo(expected)).isEqualTo(0)` para comparar `BigDecimal` (nunca `assertEquals`/`.equals()`, D-06). Como `calcularAvaluoRealAlhaja`/`buildPartida` son privados (Pitfall 3), TODAS las pruebas pasan por el metodo publico `crearContrato(ContratoRequest, String)`.

Mocks requeridos (mismo orden que el constructor de ContratoService tras el Task 1): `ContratoRepository`, `ClienteRepository`, `PlazoRepository`, `TurnoRepository`, `UsuarioRepository`, `TipoPrendaRepository`, `CatValorPrendaRepository`, `ContratoMapper`, `PlazoParametroRepository`, `PlazoService`, `PlazoHechuraAlhajaRepository`. Instanciar `contratoService` en `@BeforeEach` pasando los 11 mocks en ese orden.

Helper privado `buildRequestBase()` que retorna un `ContratoRequest` con: `idCliente=1`, `idPlazo=1L`, `partidas` vacio (cada test agrega su propia partida). Helper `buildPartidaAlhaja(BigDecimal avaluoRealSpoofed, Integer kilataje, String hechura, BigDecimal pesoGramos, BigDecimal montoPrestamo)` que retorna un `PartidaContratoRequest` con `idTipoPrenda=1`, `descripcion="Anillo de oro"`, `kilataje`, `hechura`, `pesoGramos`, `avaluoReal=avaluoRealSpoofed`, `montoPrestamo`.

Stubs comunes a reutilizar en cada test (usar `lenient()` de Mockito si algun stub no se usa en un test que corta antes por excepcion, para evitar `UnnecessaryStubbingException`):
- `Turno turno = new Turno(); turno.setId(1);` (Turno.id es Integer, confirmado en Turno.java) — `when(turnoRepository.findByActivo(true)).thenReturn(Optional.of(turno));`
- `Usuario usuario = new Usuario(); usuario.setNombreUsuario("cajero1");` — `when(usuarioRepository.findByNombreUsuario("cajero1")).thenReturn(Optional.of(usuario));`
- `Cliente cliente = new Cliente(); cliente.setId(1);` — `when(clienteRepository.findById(1)).thenReturn(Optional.of(cliente));`
- `Plazo plazo = new Plazo(); plazo.setId(1L); plazo.setDiasPorPeriodo(7); plazo.setNumeroPeriodos(10);` — `when(plazoRepository.findById(1L)).thenReturn(Optional.of(plazo));`
- `TipoPrenda alhaja = new TipoPrenda(); alhaja.setId(1); alhaja.setTipo("ALHAJA");` — `when(tipoPrendaRepository.findById(1)).thenReturn(Optional.of(alhaja));`
- `when(plazoParametroRepository.findByPlazoIdAndTipoPrendaIdAndSucursalId(1L, 1, 1)).thenReturn(Optional.empty());` (parametro null simplifica: prestamoMaximo = avaluoReal calculado tal cual, sin porcentaje adicional — ver `calcularPrestamoMaximo`)
- `when(contratoMapper.toResponse(any(Contrato.class))).thenAnswer(inv -> { /* puede devolver null o un ContratoResponse vacio; no es el foco de estas pruebas */ return new com.ignis.prestamil.response.ContratoResponse(); });`
- `when(repository.save(any(Contrato.class))).thenAnswer(inv -> { Contrato c = inv.getArgument(0); if (c.getId() == null) c.setId(1L); return c; });`

Escribir estos 4 tests:

1. `crearContrato_partidaAlhaja_ignoraAvaluoRealDelCliente()`:
   - Arrange: partida ALHAJA con `avaluoReal=999999.00` (spoofed), `kilataje=14`, `hechura="N"`, `pesoGramos=new BigDecimal("10.0000")`, `montoPrestamo=new BigDecimal("500.00")`.
   - Mockear `plazoHechuraAlhajaRepository.findById(new PlazoHechuraAlhajaId(1, 1, 14, "N"))` retornando un `PlazoHechuraAlhaja` con `precioPrestamo = new BigDecimal("1200.0000")` (de forma que el avaluo real calculado sea `1200.0000 * 10.0000 = 12000.00`, que es mayor a `montoPrestamo=500.00`, dejando pasar la validacion de prestamo maximo).
   - Act: `ContratoResponse response = contratoService.crearContrato(request, "cajero1");` (no interesa el contenido del response ya que `contratoMapper` esta mockeado — el foco es capturar el `Contrato`/`PartidaContrato` real construido).
   - Assert: capturar el `Contrato` pasado a `repository.save(...)` con `ArgumentCaptor<Contrato>`, tomar `captured.getPartidas().get(0).getAvaluoReal()`, verificar `assertThat(avaluoRealPersistido.compareTo(new BigDecimal("999999.00"))).isNotEqualTo(0);` y `assertThat(avaluoRealPersistido.compareTo(new BigDecimal("12000.00"))).isEqualTo(0);` (el valor calculado por el servidor, no el spoofed).

2. `crearContrato_partidaAlhaja_kilataje24K_rechazaConMensajeClaro()`:
   - Arrange: partida ALHAJA con `kilataje=24`, `hechura="N"`, `pesoGramos=new BigDecimal("5.0000")`, `montoPrestamo=new BigDecimal("100.00")`, `avaluoReal=new BigDecimal("1.00")`.
   - Act/Assert: `BadRequestException ex = assertThrows(BadRequestException.class, () -> contratoService.crearContrato(request, "cajero1")); assertThat(ex.getMessage()).isEqualTo("Oro de 24K no es prendable");`
   - No es necesario mockear `plazoHechuraAlhajaRepository` para este test (la excepcion se lanza antes de consultarlo).

3. `crearContrato_partidaAlhaja_kilatajeNoSoportado_rechaza()`:
   - Arrange: partida ALHAJA con `kilataje=16` (no esta en la lista COCAE), resto de campos validos.
   - Act/Assert: `BadRequestException ex = assertThrows(BadRequestException.class, () -> contratoService.crearContrato(request, "cajero1")); assertThat(ex.getMessage()).isEqualTo("Kilataje no soportado: 16");`

4. `crearContrato_partidaAlhaja_montoPrestamoSuperaMaximoCalculadoPorServidor_rechaza()`:
   - Arrange: misma celda que el test 1 (`precioPrestamo=1200.0000`, `pesoGramos=10.0000` → avaluo real servidor = 12000.00), pero con `montoPrestamo=new BigDecimal("999999.00")` (excede el avaluo real real calculado, aunque el cliente mando un `avaluoReal` spoofed de 999999.00 que "coincidiria" con su propio monto).
   - Act/Assert: `BadRequestException ex = assertThrows(BadRequestException.class, () -> contratoService.crearContrato(request, "cajero1")); assertThat(ex.getMessage()).contains("supera el máximo autorizado");` — demuestra que el techo real es 12000.00 (del servidor), no 999999.00 (del cliente).

Importar lo necesario: `com.ignis.prestamil.exception.BadRequestException`, `com.ignis.prestamil.exception.ResourceNotFoundException`, `com.ignis.prestamil.model.*`, `com.ignis.prestamil.repository.*`, `com.ignis.prestamil.request.ContratoRequest`, `com.ignis.prestamil.request.PartidaContratoRequest`, `com.ignis.prestamil.response.ContratoResponse`, `org.mockito.ArgumentCaptor`, `java.math.BigDecimal`, `java.util.List`, `java.util.Optional`, `static org.assertj.core.api.Assertions.assertThat`, `static org.junit.jupiter.api.Assertions.assertThrows`, `static org.mockito.ArgumentMatchers.any`, `static org.mockito.Mockito.*`.
  </action>
  <verify>
    <automated>cd prestamil-backend && mvn -q test -Dtest=ContratoServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn test -Dtest=ContratoServiceTest` termina con BUILD SUCCESS y 4 tests en verde
    - El archivo contiene `ignoraAvaluoRealDelCliente` y verifica `compareTo(new BigDecimal("999999.00"))).isNotEqualTo(0)`
    - El archivo contiene aserciones sobre los mensajes exactos `"Oro de 24K no es prendable"` y `"Kilataje no soportado: 16"`
    - Ningun test usa `assertEquals`/`.equals(` para comparar valores `BigDecimal` (D-06)
  </acceptance_criteria>
  <done>ContratoServiceTest.java existe, compila y pasa; demuestra que un avaluoReal spoofed por el cliente no tiene efecto en el avaluo persistido ni en el techo de prestamo, y que 24K/kilatajes no soportados se rechazan con los mensajes exactos de D-04/D-05.</done>
</task>

</tasks>

<verification>
`cd prestamil-backend && mvn -q test -Dtest=ContratoServiceTest` debe pasar en verde. `mvn -q compile` debe seguir en BUILD SUCCESS para todo el modulo. Este plan no depende de 04-01/04-02 (no toca `oro_tabla_prestamo` ni el calculo de `PlazoHechuraAlhaja.precioPrestamo` — solo consume el valor ya almacenado en esa columna, sea cual sea su origen), por lo que puede ejecutarse en paralelo con el plan 04-01.
</verification>

<success_criteria>
- ORO-03: el servidor recalcula el avaluo real de una partida ALHAJA a partir de `PlazoHechuraAlhaja` en `ContratoService.buildPartida`, ignorando `pr.getAvaluoReal()`.
- ORO-04 (parcial, cubierto en este plan para el lado ContratoService): prueba de regresion que demuestra que un `avaluoReal` spoofed por el cliente no altera ni el avaluo persistido ni el techo de prestamo.
- D-04/D-05: kilataje 24K y kilatajes no soportados se rechazan con mensajes de negocio claros, no con el error generico de "prestamo minimo".
</success_criteria>

<output>
After completion, create `.planning/phases/04-motor-de-oro-fidelidad-cocae-cierre-de-brecha-de-confianza/04-03-SUMMARY.md`
</output>
