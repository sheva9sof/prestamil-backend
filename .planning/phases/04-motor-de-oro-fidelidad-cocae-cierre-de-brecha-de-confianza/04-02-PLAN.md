---
phase: 04-motor-de-oro-fidelidad-cocae-cierre-de-brecha-de-confianza
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java
  - prestamil-backend/src/test/java/com/ignis/prestamil/service/PlazoServiceTest.java
autonomous: true
requirements: [ORO-01, ORO-02, ORO-04]

must_haves:
  truths:
    - "Al cambiar el precio base del oro, PlazoService.recalcularRegistros recalcula precioBase usando el %Prestamo real de la celda (kilataje x hechura) de oro_tabla_prestamo, no los 3 factores globales Fundir/Normal/Especial"
    - "porcAumento propio de cada celda (plazo) se conserva sin sobreescribirse durante el recalculo (ORO-02)"
    - "Los montos de precioBase/precioPrestamo calculados coinciden exactamente (compareTo) con los valores derivados de capturas reales de COCAE"
  artifacts:
    - path: "prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java"
      provides: "recalcularRegistros corregido: deriva precioBase desde oro_tabla_prestamo, ya no desde factorFundir/Normal/Especial"
      contains: "oroTablaPrestamoRepository"
    - path: "prestamil-backend/src/test/java/com/ignis/prestamil/service/PlazoServiceTest.java"
      provides: "pruebas unitarias de paridad COCAE (compareTo, nunca equals) para PlazoService"
      contains: "compareTo"
  key_links:
    - from: "prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java"
      to: "prestamil-backend/src/main/java/com/ignis/prestamil/repository/OroTablaPrestamoRepository.java"
      via: "oroTablaPrestamoRepository.findByIdSucursalId(sucursalId) dentro de recalcularRegistros"
      pattern: "oroTablaPrestamoRepository\\.findByIdSucursalId"
---

<objective>
Corregir `PlazoService.recalcularRegistros` para que derive `precioBase` de cada celda (kilataje x hechura) desde la tabla real de 24 celdas de COCAE (`oro_tabla_prestamo`, creada en el plan 04-01) en vez de multiplicar por uno de 3 factores globales (Fundir/Normal/Especial). El `porcAumento` propio de cada celda no debe tocarse (ORO-02, ya correcto hoy — no regresionar).

Purpose: Cierra ORO-01 (la tabla de 24 celdas reemplaza el calculo por 3 factores globales) y ORO-02 (preservar `porcAumento` por celda). Sin este fix, todo el motor de oro sigue produciendo montos distintos a COCAE aunque la tabla de datos ya exista.
Output: `PlazoService.recalcularRegistros` reescrito segun la formula confirmada de COCAE, con `OroTablaPrestamoRepository` inyectado; `PlazoServiceTest.java` nuevo con pruebas de paridad exacta contra capturas reales (compareTo, nunca equals).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-motor-de-oro-fidelidad-cocae-cierre-de-brecha-de-confianza/04-CONTEXT.md
@.planning/phases/04-motor-de-oro-fidelidad-cocae-cierre-de-brecha-de-confianza/04-RESEARCH.md
@prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java
@prestamil-backend/src/test/java/com/ignis/prestamil/service/UsuarioServiceTest.java

<interfaces>
<!-- Del plan 04-01 (ya debe existir en disco al ejecutar este plan) -->

From prestamil-backend/src/main/java/com/ignis/prestamil/model/OroTablaPrestamoId.java:
```java
@Embeddable
public class OroTablaPrestamoId implements Serializable {
    private Integer sucursalId;
    private Integer kilataje;
    private String hechura; // "F"/"N"/"E"
}
```

From prestamil-backend/src/main/java/com/ignis/prestamil/model/OroTablaPrestamo.java:
```java
@Entity
@Table(name = "oro_tabla_prestamo")
public class OroTablaPrestamo {
    @EmbeddedId
    private OroTablaPrestamoId id;
    private BigDecimal porcPrestamo; // precision 7, scale 4
    private LocalDateTime actualizadoEn;
}
```

From prestamil-backend/src/main/java/com/ignis/prestamil/repository/OroTablaPrestamoRepository.java:
```java
public interface OroTablaPrestamoRepository extends BaseRepository<OroTablaPrestamo, OroTablaPrestamoId> {
    List<OroTablaPrestamo> findByIdSucursalId(Integer sucursalId);
}
```

<!-- Estado ACTUAL (incorrecto) de PlazoService — leer el archivo completo antes de editar -->
Constructor actual (agregar oroTablaPrestamoRepository como ultimo parametro, sin romper el orden existente):
```java
public PlazoService(PlazoRepository repository,
                    PlazoMapper plazoMapper,
                    TipoPrendaService tipoPrendaService,
                    PlazoParametroRepository plazoParametroRepository,
                    PlazoParametroMapper plazoParametroMapper,
                    PlazoHechuraAlhajaRepository plazoHechuraAlhajaRepository,
                    PlazoHechuraAlhajaMapper plazoHechuraAlhajaMapper,
                    com.ignis.prestamil.repository.PrecioOroRepository precioOroRepository) { ... }
```

Metodo actual (BUGGY — usa 3 factores globales, sera reemplazado):
```java
private void recalcularRegistros(List<PlazoHechuraAlhaja> registros,
                                 BigDecimal precioGramoBase, int baseKilataje,
                                 BigDecimal factorFundir, BigDecimal factorNormal, BigDecimal factorEspecial) {
    BigDecimal base = new BigDecimal(baseKilataje);
    BigDecimal precioPorKilatePuro = precioGramoBase.divide(base, 10, RoundingMode.HALF_UP);
    for (PlazoHechuraAlhaja r : registros) {
        BigDecimal factorHechura = factorPorHechura(r.getId().getHechura(),
                factorFundir, factorNormal, factorEspecial)
                .divide(CIEN, 10, RoundingMode.HALF_UP);
        BigDecimal precioBase = precioPorKilatePuro
                .multiply(new BigDecimal(r.getId().getKilataje()))
                .multiply(factorHechura)
                .setScale(4, RoundingMode.HALF_UP);
        r.setPrecioBase(precioBase);
        r.setPrecioPrestamo(precioBase
                .multiply(BigDecimal.ONE.add(r.getPorcAumento().divide(CIEN, 10, RoundingMode.HALF_UP)))
                .setScale(4, RoundingMode.HALF_UP));
    }
}
```

Llamador 1 (actualizarTodosPrecios) actual — leer bloque completo en el archivo antes de editar:
```java
public void actualizarTodosPrecios(Integer idPlazo, Integer sucursalId, BigDecimal precioBaseOro) {
    // ...
    com.ignis.prestamil.model.PrecioOro precio = precioOroRepository.findBySucursalId(sucursalId).orElse(null);
    int baseKilataje = precio != null && precio.getBaseKilataje() != null ? precio.getBaseKilataje() : 24;
    BigDecimal factorFundir = precio != null && precio.getFactorFundir() != null ? precio.getFactorFundir() : new BigDecimal("90.0000");
    BigDecimal factorNormal = precio != null && precio.getFactorNormal() != null ? precio.getFactorNormal() : new BigDecimal("100.0000");
    BigDecimal factorEspecial = precio != null && precio.getFactorEspecial() != null ? precio.getFactorEspecial() : new BigDecimal("110.0000");
    recalcularRegistros(registros, precioBaseOro, baseKilataje, factorFundir, factorNormal, factorEspecial);
    plazoHechuraAlhajaRepository.saveAll(registros);
}
```

Llamador 2 (recalcularTodasLasTablas) actual — leer bloque completo en el archivo antes de editar. Resuelve factorFundir/Normal/Especial y los PERSISTE en PrecioOro (eso se conserva, D-02); solo deja de pasarlos a recalcularRegistros.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reescribir recalcularRegistros usando oro_tabla_prestamo (ORO-01, ORO-02)</name>
  <files>prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java</files>
  <read_first>
    prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java
    prestamil-backend/src/main/java/com/ignis/prestamil/model/PlazoHechuraAlhaja.java
    prestamil-backend/src/main/java/com/ignis/prestamil/model/OroTablaPrestamo.java
    prestamil-backend/src/main/java/com/ignis/prestamil/model/OroTablaPrestamoId.java
    prestamil-backend/src/main/java/com/ignis/prestamil/repository/OroTablaPrestamoRepository.java
  </read_first>
  <behavior>
    - Dado precioGramoBase=1679.50, baseKilataje=21, celda 21K/Normal con porcPrestamo=63.4400 (de oro_tabla_prestamo) y porcAumento propio de la fila = 10.0000 (no debe sobreescribirse): precioBase resultante debe ser 1065.4748 y precioPrestamo resultante debe ser 1172.0223 (compareTo == 0, nunca equals)
    - El porcAumento de la fila permanece 10.0000 despues de recalcularRegistros (ORO-02 — no se deriva de ningun factor global)
    - Si se invoca con factorFundir/factorNormal/factorEspecial de PrecioOro distintos a los valores por defecto (p.ej. 50/100/150), el resultado de precioBase/precioPrestamo NO cambia — esos factores ya no participan en el calculo de la tabla de alhajas (solo se siguen persistiendo en PrecioOro para la pantalla "Precio del Oro", D-02)
    - Si no existe fila en oro_tabla_prestamo para una celda (kilataje/hechura) presente en plazo_hechura_alhaja, se lanza ResourceNotFoundException con mensaje que incluya sucursalId, kilataje y hechura
  </behavior>
  <action>
En `prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java`:

1. Agregar imports: `com.ignis.prestamil.model.OroTablaPrestamo`, `com.ignis.prestamil.repository.OroTablaPrestamoRepository`, `java.util.Map`, `java.util.stream.Collectors`.

2. Agregar el campo `private final OroTablaPrestamoRepository oroTablaPrestamoRepository;` y agregarlo como ULTIMO parametro del constructor (no reordenar los parametros existentes), asignandolo en el cuerpo del constructor igual que los demas campos `final`.

3. Reemplazar el metodo `recalcularRegistros` completo por:

```java
    /**
     * Aplica la formula real de COCAE a una lista de registros (sin persistir):
     *   Paso 1 (intermedio, no se guarda): precioPorKilatePuro = precioGramoBase / baseKilataje
     *   Paso 2: precioAvaluo(kilate) = precioPorKilatePuro * kilataje
     *   Paso 3: precioBase(kilate,hechura) = precioAvaluo * %Prestamo(kilate,hechura) / 100
     *           — %Prestamo viene de la tabla global oro_tabla_prestamo (24 celdas, irregular por diseno de COCAE)
     *   Paso 4: precioPrestamo(kilate,hechura,plazo) = precioBase * (1 + porcAumento_propio_de_la_celda / 100)
     *           — porcAumento NO se deriva ni se sobreescribe aqui; es el valor ya almacenado en la fila (ORO-02)
     * Contrato de redondeo (D-06): pasos intermedios usan escala 10 HALF_UP (nunca se persisten);
     * precioBase y precioPrestamo se persisten con escala 4 HALF_UP (coincide con DECIMAL(12,4) del schema).
     *
     * @param registros  registros de hechura/kilataje a recalcular
     * @param precioGramoBase precio del oro por gramo al kilataje base
     * @param baseKilataje kilataje de referencia del precio ingresado (21 o 24)
     * @param sucursalId identificador de la sucursal, usado para resolver %Prestamo en oro_tabla_prestamo
     * @throws ResourceNotFoundException si falta la celda (kilataje,hechura) en oro_tabla_prestamo para la sucursal
     */
    private void recalcularRegistros(List<PlazoHechuraAlhaja> registros,
                                      BigDecimal precioGramoBase, int baseKilataje,
                                      Integer sucursalId) {
        BigDecimal base = new BigDecimal(baseKilataje);
        BigDecimal precioPorKilatePuro = precioGramoBase.divide(base, 10, RoundingMode.HALF_UP);

        Map<String, BigDecimal> porcPrestamoPorCelda = oroTablaPrestamoRepository
                .findByIdSucursalId(sucursalId).stream()
                .collect(Collectors.toMap(
                        r -> r.getId().getKilataje() + "-" + r.getId().getHechura(),
                        OroTablaPrestamo::getPorcPrestamo));

        for (PlazoHechuraAlhaja r : registros) {
            String celda = r.getId().getKilataje() + "-" + r.getId().getHechura();
            BigDecimal porcPrestamo = porcPrestamoPorCelda.get(celda);
            if (porcPrestamo == null) {
                throw new ResourceNotFoundException(
                    "No hay %Prestamo COCAE configurado para sucursal=" + sucursalId
                    + ", kilataje=" + r.getId().getKilataje() + ", hechura=" + r.getId().getHechura());
            }
            BigDecimal precioAvaluo = precioPorKilatePuro.multiply(new BigDecimal(r.getId().getKilataje()));
            BigDecimal precioBase = precioAvaluo
                    .multiply(porcPrestamo.divide(CIEN, 10, RoundingMode.HALF_UP))
                    .setScale(4, RoundingMode.HALF_UP);
            r.setPrecioBase(precioBase);
            r.setPrecioPrestamo(precioBase
                    .multiply(BigDecimal.ONE.add(r.getPorcAumento().divide(CIEN, 10, RoundingMode.HALF_UP)))
                    .setScale(4, RoundingMode.HALF_UP));
        }
    }
```

4. Eliminar el metodo privado `factorPorHechura(...)` completo — queda muerto tras el reemplazo (ya no se usa en ningun otro lugar del archivo; confirmado por grep).

5. En `actualizarTodosPrecios(Integer idPlazo, Integer sucursalId, BigDecimal precioBaseOro)`: eliminar la resolucion de `factorFundir`/`factorNormal`/`factorEspecial` (esas 3 lineas que leen `precio.getFactorFundir()/getFactorNormal()/getFactorEspecial()`); mantener la resolucion de `baseKilataje` desde `precio.getBaseKilataje()` igual que hoy. Cambiar la llamada a:
```java
recalcularRegistros(registros, precioBaseOro, baseKilataje, sucursalId);
```

6. En `recalcularTodasLasTablas(Integer sucursalId, PrecioOroRequest request, String usuario)`: MANTENER la resolucion de `factorFundir`/`factorNormal`/`factorEspecial` (siguen persistiendose en `PrecioOro` — D-02, la pantalla "Precio del Oro" los sigue usando como referencia), pero cambiar la llamada al metodo interno a:
```java
recalcularRegistros(registros, request.getPrecioGramoBase(), baseKilataje, sucursalId);
```
No tocar el bloque que hace `precio.setFactorFundir(...)`/`setFactorNormal(...)`/`setFactorEspecial(...)` mas abajo en el mismo metodo — eso se conserva intacto.

7. No modificar `actualizarPrecioBase`, `crearAlhaja`, `calcularAvaluoContrato`, ni ningun otro metodo publico de la clase — su firma y comportamiento no cambian.
  </action>
  <verify>
    <automated>cd prestamil-backend && mvn -q compile</automated>
  </verify>
  <acceptance_criteria>
    - `mvn compile` en `prestamil-backend` termina con BUILD SUCCESS
    - `PlazoService.java` contiene la cadena `oroTablaPrestamoRepository`
    - `PlazoService.java` ya NO contiene la cadena `factorPorHechura`
    - La firma de `recalcularRegistros` en el archivo tiene exactamente 4 parametros: `List<PlazoHechuraAlhaja> registros, BigDecimal precioGramoBase, int baseKilataje, Integer sucursalId` (grep del `private void recalcularRegistros(`)
    - `recalcularTodasLasTablas` sigue conteniendo `precio.setFactorFundir(` (los factores se siguen persistiendo para la pantalla Precio del Oro, D-02)
  </acceptance_criteria>
  <done>recalcularRegistros deriva precioBase desde oro_tabla_prestamo (24 celdas reales de COCAE) en vez de 3 factores globales; porcAumento por celda se preserva; ambos llamadores (actualizarTodosPrecios, recalcularTodasLasTablas) compilan y siguen persistiendo PrecioOro sin cambios de comportamiento visible en esa pantalla.</done>
</task>

<task type="auto">
  <name>Task 2: PlazoServiceTest — paridad exacta contra capturas reales de COCAE</name>
  <files>prestamil-backend/src/test/java/com/ignis/prestamil/service/PlazoServiceTest.java</files>
  <read_first>
    prestamil-backend/src/test/java/com/ignis/prestamil/service/UsuarioServiceTest.java
    prestamil-backend/src/main/java/com/ignis/prestamil/service/PlazoService.java
  </read_first>
  <action>
Crear `prestamil-backend/src/test/java/com/ignis/prestamil/service/PlazoServiceTest.java` (archivo nuevo, no existe ningun test para esta clase hoy). Seguir EXACTAMENTE el estilo de `UsuarioServiceTest.java`: `@ExtendWith(MockitoExtension.class)`, mocks con `@Mock`, instanciacion manual del servicio en `@BeforeEach` via constructor (NO `@InjectMocks`), aserciones con AssertJ (`assertThat`), NUNCA `assertEquals`/`.equals()` sobre `BigDecimal` — siempre `assertThat(actual.compareTo(expected)).isEqualTo(0)` (D-06).

Mocks requeridos (mismo orden que el constructor de PlazoService tras el Task 1): `PlazoRepository`, `PlazoMapper`, `TipoPrendaService`, `PlazoParametroRepository`, `PlazoParametroMapper`, `PlazoHechuraAlhajaRepository`, `PlazoHechuraAlhajaMapper`, `PrecioOroRepository` (`com.ignis.prestamil.repository.PrecioOroRepository`), `OroTablaPrestamoRepository`. Instanciar `plazoService` en `@BeforeEach` pasando los 9 mocks en ese orden.

Escribir estos 4 tests (usar `import static org.assertj.core.api.Assertions.assertThat;`, `import org.mockito.ArgumentCaptor;`, `import java.util.List; import java.util.Optional; import java.math.BigDecimal;`):

1. `actualizarTodosPrecios_21K_Normal_coincideConCOCAE()`:
   - Arrange: una fila `PlazoHechuraAlhaja` con `id = new PlazoHechuraAlhajaId(1, 1, 21, "N")` y `porcAumento = new BigDecimal("10.0000")`.
   - `when(plazoHechuraAlhajaRepository.findByIdIdPlazoAndIdSucursalId(1, 1)).thenReturn(List.of(fila21N));`
   - Una fila `OroTablaPrestamo` con `id = new OroTablaPrestamoId(1, 21, "N")` y `porcPrestamo = new BigDecimal("63.4400")`.
   - `when(oroTablaPrestamoRepository.findByIdSucursalId(1)).thenReturn(List.of(celda21N));`
   - Un `PrecioOro` mock con `getBaseKilataje()` retornando `21` (usar `Optional.of(precioOroConBase21)` en `precioOroRepository.findBySucursalId(1)`); no es necesario mockear `getFactorFundir/Normal/Especial` en este test (ya no se usan en `actualizarTodosPrecios`).
   - Act: `plazoService.actualizarTodosPrecios(1, 1, new BigDecimal("1679.50"));`
   - Assert: capturar el `List<PlazoHechuraAlhaja>` pasado a `plazoHechuraAlhajaRepository.saveAll(...)` con `ArgumentCaptor`, verificar `precioBase.compareTo(new BigDecimal("1065.4748")) == 0`, `precioPrestamo.compareTo(new BigDecimal("1172.0223")) == 0`, y `porcAumento.compareTo(new BigDecimal("10.0000")) == 0` (ORO-02: no se sobreescribe).

2. `actualizarTodosPrecios_ignoraFactoresHechuraGlobales()`: mismo arrange que el test 1 pero con un `PrecioOro` cuyos `factorFundir/factorNormal/factorEspecial` son valores distintos a los defaults (p.ej. 50/100/150) — como `actualizarTodosPrecios` ya no lee esos campos, no hace falta stubbearlos; el punto del test es reafirmar que el resultado (`precioBase`/`precioPrestamo`) es identico al test 1 aunque el `PrecioOro` mockeado tenga esos campos con valores no default. Mismas aserciones que el test 1.

3. `actualizarTodosPrecios_celdaSinPorcPrestamo_lanzaResourceNotFoundException()`:
   - Arrange: una fila `PlazoHechuraAlhaja` con `id = new PlazoHechuraAlhajaId(1, 1, 14, "F")`.
   - `oroTablaPrestamoRepository.findByIdSucursalId(1)` retorna `List.of()` (tabla vacia para esa sucursal).
   - Act/Assert: `assertThrows(ResourceNotFoundException.class, () -> plazoService.actualizarTodosPrecios(1, 1, new BigDecimal("1679.50")));`

4. `actualizarTodosPrecios_registrosVacios_lanzaResourceNotFoundException()`:
   - Arrange: `plazoHechuraAlhajaRepository.findByIdIdPlazoAndIdSucursalId(1, 1)` retorna `List.of()`.
   - Act/Assert: `assertThrows(ResourceNotFoundException.class, () -> plazoService.actualizarTodosPrecios(1, 1, new BigDecimal("1679.50")));` (este comportamiento ya existia antes del fix — es una prueba de no-regresion).

Importar `com.ignis.prestamil.exception.ResourceNotFoundException`, `org.junit.jupiter.api.Assertions.assertThrows`, `com.ignis.prestamil.model.PlazoHechuraAlhaja`, `com.ignis.prestamil.model.PlazoHechuraAlhajaId`, `com.ignis.prestamil.model.OroTablaPrestamo`, `com.ignis.prestamil.model.OroTablaPrestamoId`, `com.ignis.prestamil.model.PrecioOro`, `com.ignis.prestamil.repository.*`.
  </action>
  <verify>
    <automated>cd prestamil-backend && mvn -q test -Dtest=PlazoServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn test -Dtest=PlazoServiceTest` termina con BUILD SUCCESS y 4 tests en verde
    - El archivo contiene `compareTo` y NO contiene `assertEquals` ni `.equals(` aplicado sobre valores `BigDecimal` (D-06)
    - El test 1 verifica explicitamente que `porcAumento` permanece `10.0000` tras el recalculo (ORO-02)
  </acceptance_criteria>
  <done>PlazoServiceTest.java existe, compila y pasa; demuestra paridad exacta (compareTo) contra los valores confirmados de COCAE (precioBase=1065.4748, precioPrestamo=1172.0223 para 21K/Normal con precio base 1679.50) y que porcAumento no se sobreescribe.</done>
</task>

</tasks>

<verification>
`cd prestamil-backend && mvn -q test -Dtest=PlazoServiceTest` debe pasar en verde. `mvn -q compile` debe seguir en BUILD SUCCESS para todo el modulo (no romper otros consumidores de `PlazoService`, p.ej. `ContratoService` en el plan 04-03, que solo depende de metodos publicos no tocados aqui).
</verification>

<success_criteria>
- ORO-01: la formula de calculo real usa la tabla de 24 celdas (`oro_tabla_prestamo`) en vez de 3 factores globales.
- ORO-02: `porcAumento` propio de cada celda se preserva durante el recalculo.
- ORO-04 (parcial, cubierto en este plan para el lado PlazoService): pruebas unitarias con `compareTo` contra un valor derivado de una captura real de COCAE (21K/Normal, precio base 1679.50) pasan.
</success_criteria>

<output>
After completion, create `.planning/phases/04-motor-de-oro-fidelidad-cocae-cierre-de-brecha-de-confianza/04-02-SUMMARY.md`
</output>
