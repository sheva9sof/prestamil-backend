---
phase: 04-motor-de-oro-fidelidad-cocae-cierre-de-brecha-de-confianza
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prestamil-backend/src/main/resources/db/changelog/changes/012-oro-tabla-prestamo-cocae.sql
  - prestamil-backend/src/main/resources/db/changelog/db.changelog-master.xml
  - prestamil-backend/src/main/java/com/ignis/prestamil/model/OroTablaPrestamoId.java
  - prestamil-backend/src/main/java/com/ignis/prestamil/model/OroTablaPrestamo.java
  - prestamil-backend/src/main/java/com/ignis/prestamil/repository/OroTablaPrestamoRepository.java
autonomous: true
requirements: [ORO-01]

must_haves:
  truths:
    - "El sistema tiene persistida la tabla real de COCAE (24 celdas: 8 kilates x 3 hechuras) como datos, no como código Java hardcodeado"
    - "La tabla es global por sucursal (no varía por plazo), consistente con D-01"
  artifacts:
    - path: "prestamil-backend/src/main/resources/db/changelog/changes/012-oro-tabla-prestamo-cocae.sql"
      provides: "changeset Liquibase que crea oro_tabla_prestamo e importa las 24 filas confirmadas de COCAE"
      contains: "CREATE TABLE oro_tabla_prestamo"
    - path: "prestamil-backend/src/main/java/com/ignis/prestamil/model/OroTablaPrestamo.java"
      provides: "entidad JPA de solo lectura para la tabla global de %Prestamo"
      contains: "@EmbeddedId"
    - path: "prestamil-backend/src/main/java/com/ignis/prestamil/repository/OroTablaPrestamoRepository.java"
      provides: "acceso de lectura a oro_tabla_prestamo por sucursal, consumido por PlazoService en el plan 04-02"
      exports: ["findByIdSucursalId"]
  key_links:
    - from: "prestamil-backend/src/main/resources/db/changelog/db.changelog-master.xml"
      to: "prestamil-backend/src/main/resources/db/changelog/changes/012-oro-tabla-prestamo-cocae.sql"
      via: "<include file=...>"
      pattern: "012-oro-tabla-prestamo-cocae.sql"
---

<objective>
Crear la infraestructura de datos para la tabla real de COCAE de 24 celdas (8 kilates x 3 hechuras = %Prestamo), que hoy no existe en el sistema — el cálculo actual usa 3 factores globales (Fundir/Normal/Especial) en vez de esta tabla irregular real.

Purpose: Esta tabla es la fuente de verdad que el plan 04-02 usará para corregir `PlazoService.recalcularRegistros`, reemplazando el cálculo por 3 factores globales por el lookup real de COCAE (ORO-01). Es infraestructura pura — no cambia ningún comportamiento todavía (el wiring ocurre en 04-02).
Output: Changeset Liquibase `012-oro-tabla-prestamo-cocae.sql` (tabla + 24 filas), registrado en `db.changelog-master.xml`; entidad JPA `OroTablaPrestamo`/`OroTablaPrestamoId` y su repositorio, siguiendo exactamente el patrón ya establecido por `PlazoHechuraAlhaja`/`PlazoHechuraAlhajaId`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-motor-de-oro-fidelidad-cocae-cierre-de-brecha-de-confianza/04-CONTEXT.md
@.planning/phases/04-motor-de-oro-fidelidad-cocae-cierre-de-brecha-de-confianza/04-RESEARCH.md

<interfaces>
<!-- Patrón exacto a replicar, extraído de la entidad hermana PlazoHechuraAlhaja/PlazoHechuraAlhajaId -->

From prestamil-backend/src/main/java/com/ignis/prestamil/model/PlazoHechuraAlhajaId.java:
```java
@Embeddable
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
public class PlazoHechuraAlhajaId implements Serializable {
    @Column(name = "id_plazo", nullable = false)
    private Integer idPlazo;
    @Column(name = "sucursal_id", nullable = false)
    private Integer sucursalId;
    @Column(name = "kilataje", nullable = false)
    private Integer kilataje;
    @Column(name = "hechura", nullable = false, length = 1)
    private String hechura;
    // equals()/hashCode() sobre los 4 campos (Objects.equals / Objects.hash)
}
```

From prestamil-backend/src/main/java/com/ignis/prestamil/model/PlazoHechuraAlhaja.java:
```java
@Entity
@Table(name = "plazo_hechura_alhaja")
@Getter @Setter @NoArgsConstructor @AllArgsConstructor
public class PlazoHechuraAlhaja {
    @EmbeddedId
    private PlazoHechuraAlhajaId id;
    @Column(name = "precio_base", nullable = false, precision = 12, scale = 4)
    private BigDecimal precioBase;
    // ...
}
```

From prestamil-backend/src/main/java/com/ignis/prestamil/repository/PlazoHechuraAlhajaRepository.java:
```java
public interface PlazoHechuraAlhajaRepository
        extends BaseRepository<PlazoHechuraAlhaja, PlazoHechuraAlhajaId> {
    List<PlazoHechuraAlhaja> findByIdSucursalId(Integer sucursalId);
}
```

From prestamil-backend/src/main/java/com/ignis/prestamil/repository/BaseRepository.java:
```java
@NoRepositoryBean
public interface BaseRepository<T, ID> extends JpaRepository<T, ID> {
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Changeset Liquibase 012 — tabla oro_tabla_prestamo + 24 filas COCAE</name>
  <files>
    prestamil-backend/src/main/resources/db/changelog/changes/012-oro-tabla-prestamo-cocae.sql
    prestamil-backend/src/main/resources/db/changelog/db.changelog-master.xml
  </files>
  <read_first>
    prestamil-backend/src/main/resources/db/changelog/changes/011-oro-sancion-plata.sql
    prestamil-backend/src/main/resources/db/changelog/db.changelog-master.xml
  </read_first>
  <action>
Crear el archivo `prestamil-backend/src/main/resources/db/changelog/changes/012-oro-tabla-prestamo-cocae.sql` con exactamente este contenido (formato `--liquibase formatted sql`, dos changesets, mismo estilo que `011-oro-sancion-plata.sql`):

```sql
--liquibase formatted sql

--changeset emm-a:012-1
--comment: Tabla global de %Prestamo COCAE (8 kilates x 3 hechuras) por sucursal.
--          Fuente de verdad para derivar precio_base en plazo_hechura_alhaja;
--          reemplaza el uso de factor_fundir/factor_normal/factor_especial de precio_oro
--          para ese proposito (esos factores se conservan como valores de referencia
--          en la pantalla "Precio del Oro", ver D-02, pero dejan de aplicarse en el calculo).
CREATE TABLE oro_tabla_prestamo (
  sucursal_id     INT NOT NULL,
  kilataje        INT NOT NULL,
  hechura         VARCHAR(1) NOT NULL,
  porc_prestamo   DECIMAL(7,4) NOT NULL,
  actualizado_en  DATETIME NOT NULL,
  PRIMARY KEY (sucursal_id, kilataje, hechura),
  CONSTRAINT fk_oro_tabla_prestamo_sucursal FOREIGN KEY (sucursal_id) REFERENCES sucursal(id)
);

--changeset emm-a:012-2
--comment: Import de valores reales COCAE v3.80 (capturas DIARIO tabla 7 / SEMANAL tabla 8,
--          precio base 21K = 1679.50) - sucursal 1 Tierra Colorada. 24K = 0% (D-04, no prendable).
INSERT INTO oro_tabla_prestamo (sucursal_id, kilataje, hechura, porc_prestamo, actualizado_en) VALUES
  (1, 6,  'F', 24.7600, NOW()), (1, 6,  'N', 26.7900, NOW()), (1, 6,  'E', 30.7300, NOW()),
  (1, 8,  'F', 60.1100, NOW()), (1, 8,  'N', 62.0400, NOW()), (1, 8,  'E', 64.1000, NOW()),
  (1, 10, 'F', 61.0500, NOW()), (1, 10, 'N', 62.6300, NOW()), (1, 10, 'E', 64.1500, NOW()),
  (1, 12, 'F', 61.6300, NOW()), (1, 12, 'N', 62.9500, NOW()), (1, 12, 'E', 64.2400, NOW()),
  (1, 14, 'F', 62.1400, NOW()), (1, 14, 'N', 63.2700, NOW()), (1, 14, 'E', 64.3900, NOW()),
  (1, 18, 'F', 62.5200, NOW()), (1, 18, 'N', 63.4000, NOW()), (1, 18, 'E', 66.3400, NOW()),
  (1, 21, 'F', 62.6700, NOW()), (1, 21, 'N', 63.4400, NOW()), (1, 21, 'E', 66.0800, NOW()),
  (1, 24, 'F', 0.0000,  NOW()), (1, 24, 'N', 0.0000,  NOW()), (1, 24, 'E', 0.0000,  NOW());
```

Registrar el nuevo changeset en `prestamil-backend/src/main/resources/db/changelog/db.changelog-master.xml`, agregando esta linea inmediatamente despues de la linea `<include file="db/changelog/changes/011-oro-sancion-plata.sql"/>` y antes del cierre `</databaseChangeLog>`:

```xml
    <include file="db/changelog/changes/012-oro-tabla-prestamo-cocae.sql"/>
```

No modificar ningun otro changeset existente. No tocar `plazo_hechura_alhaja` ni `precio_oro` en este changeset — esos permanecen intactos (D-02).
  </action>
  <verify>
    <automated>grep -c "changeset emm-a:012" prestamil-backend/src/main/resources/db/changelog/changes/012-oro-tabla-prestamo-cocae.sql</automated>
  </verify>
  <acceptance_criteria>
    - `012-oro-tabla-prestamo-cocae.sql` existe y contiene la cadena `CREATE TABLE oro_tabla_prestamo`
    - El archivo contiene exactamente 24 filas de INSERT (verificable con `grep -o "(1, [0-9]*, *'[FNE]'" prestamil-backend/src/main/resources/db/changelog/changes/012-oro-tabla-prestamo-cocae.sql | wc -l` → 24)
    - `db.changelog-master.xml` contiene la linea `<include file="db/changelog/changes/012-oro-tabla-prestamo-cocae.sql"/>`
    - Los changesets existentes 001-011 en `db.changelog-master.xml` no fueron modificados (mismo orden, mismas lineas)
  </acceptance_criteria>
  <done>Changeset 012 creado con tabla + 24 filas COCAE confirmadas, registrado en el master changelog, sin tocar changesets previos.</done>
</task>

<task type="auto">
  <name>Task 2: Entidad JPA OroTablaPrestamo + repositorio de solo lectura</name>
  <files>
    prestamil-backend/src/main/java/com/ignis/prestamil/model/OroTablaPrestamoId.java
    prestamil-backend/src/main/java/com/ignis/prestamil/model/OroTablaPrestamo.java
    prestamil-backend/src/main/java/com/ignis/prestamil/repository/OroTablaPrestamoRepository.java
  </files>
  <read_first>
    prestamil-backend/src/main/java/com/ignis/prestamil/model/PlazoHechuraAlhajaId.java
    prestamil-backend/src/main/java/com/ignis/prestamil/model/PlazoHechuraAlhaja.java
    prestamil-backend/src/main/java/com/ignis/prestamil/repository/PlazoHechuraAlhajaRepository.java
    prestamil-backend/src/main/java/com/ignis/prestamil/repository/BaseRepository.java
  </read_first>
  <action>
Crear `prestamil-backend/src/main/java/com/ignis/prestamil/model/OroTablaPrestamoId.java`, replicando EXACTAMENTE el patron de `PlazoHechuraAlhajaId` (mismos imports, mismas anotaciones Lombok, mismo estilo de `equals`/`hashCode`), pero con 3 campos en vez de 4:

```java
package com.ignis.prestamil.model;

import jakarta.persistence.Column;
import jakarta.persistence.Embeddable;
import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;

import java.io.Serializable;
import java.util.Objects;

@Embeddable
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
public class OroTablaPrestamoId implements Serializable {

    @Column(name = "sucursal_id", nullable = false)
    private Integer sucursalId;

    @Column(name = "kilataje", nullable = false)
    private Integer kilataje;

    @Column(name = "hechura", nullable = false, length = 1)
    private String hechura; // "F" fina, "N" normal, "E" especial

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof OroTablaPrestamoId that)) return false;
        return Objects.equals(sucursalId, that.sucursalId)
            && Objects.equals(kilataje, that.kilataje)
            && Objects.equals(hechura, that.hechura);
    }

    @Override
    public int hashCode() {
        return Objects.hash(sucursalId, kilataje, hechura);
    }
}
```

Crear `prestamil-backend/src/main/java/com/ignis/prestamil/model/OroTablaPrestamo.java`:

```java
package com.ignis.prestamil.model;

import jakarta.persistence.Column;
import jakarta.persistence.EmbeddedId;
import jakarta.persistence.Entity;
import jakarta.persistence.Table;
import lombok.Getter;
import lombok.Setter;

import java.math.BigDecimal;
import java.time.LocalDateTime;

/**
 * Tabla global de %Prestamo COCAE (8 kilates x 3 hechuras) por sucursal.
 * Fuente de verdad de solo lectura para derivar precio_base en PlazoHechuraAlhaja
 * (ver PlazoService.recalcularRegistros). No tiene endpoint ni pantalla de edicion
 * en este phase (D-02/D-03) — se puebla unicamente via changeset Liquibase.
 */
@Entity
@Table(name = "oro_tabla_prestamo")
@Getter
@Setter
public class OroTablaPrestamo {

    @EmbeddedId
    private OroTablaPrestamoId id;

    @Column(name = "porc_prestamo", nullable = false, precision = 7, scale = 4)
    private BigDecimal porcPrestamo;

    @Column(name = "actualizado_en", nullable = false)
    private LocalDateTime actualizadoEn;
}
```

Crear `prestamil-backend/src/main/java/com/ignis/prestamil/repository/OroTablaPrestamoRepository.java`:

```java
package com.ignis.prestamil.repository;

import com.ignis.prestamil.model.OroTablaPrestamo;
import com.ignis.prestamil.model.OroTablaPrestamoId;

import java.util.List;

public interface OroTablaPrestamoRepository
        extends BaseRepository<OroTablaPrestamo, OroTablaPrestamoId> {

    /**
     * Busca las 24 celdas de %Prestamo de una sucursal (todos los kilates/hechuras).
     * Usado por PlazoService.recalcularRegistros para derivar precio_base por celda.
     *
     * @param sucursalId identificador de la sucursal
     * @return lista de OroTablaPrestamo de la sucursal
     */
    List<OroTablaPrestamo> findByIdSucursalId(Integer sucursalId);
}
```

No crear mapper, DTO ni controller para esta entidad — es de solo lectura desde la aplicacion en este phase (D-02/D-03).
  </action>
  <verify>
    <automated>cd prestamil-backend && mvn -q compile</automated>
  </verify>
  <acceptance_criteria>
    - `mvn compile` en `prestamil-backend` termina con BUILD SUCCESS
    - `OroTablaPrestamo.java` contiene `@EmbeddedId` y `@Table(name = "oro_tabla_prestamo")`
    - `OroTablaPrestamoId.java` contiene los 3 campos `sucursalId`, `kilataje`, `hechura` con `equals`/`hashCode` propios
    - `OroTablaPrestamoRepository.java` extiende `BaseRepository<OroTablaPrestamo, OroTablaPrestamoId>` y expone `findByIdSucursalId(Integer sucursalId)`
  </acceptance_criteria>
  <done>Entidad, clave compuesta y repositorio de OroTablaPrestamo compilan y siguen exactamente el patron de PlazoHechuraAlhaja, listos para ser inyectados en PlazoService en el plan 04-02.</done>
</task>

</tasks>

<verification>
`cd prestamil-backend && mvn -q compile` debe terminar en BUILD SUCCESS. El changeset 012 debe quedar registrado en el master changelog sin alterar el orden ni el contenido de los changesets 001-011. Ningun endpoint ni pantalla existente cambia de comportamiento en este plan — es infraestructura pura consumida recien en el plan 04-02.
</verification>

<success_criteria>
- `oro_tabla_prestamo` existe como changeset Liquibase numerado (012) con las 24 filas confirmadas de COCAE.
- La entidad `OroTablaPrestamo`/`OroTablaPrestamoId` y su repositorio compilan y estan listos para ser consumidos por `PlazoService` (plan 04-02).
</success_criteria>

<output>
After completion, create `.planning/phases/04-motor-de-oro-fidelidad-cocae-cierre-de-brecha-de-confianza/04-01-SUMMARY.md`
</output>
